# DEUDA TÉCNICA Y FUNCIONAL — Belleza App / GlowApp

**Generado:** 2026-09-24 · **Naturaleza:** consolidación documental (no se ejecutó código ni se tocó el repositorio) · **Ámbito:** todo lo que las auditorías existentes ya habían registrado.

**Fuentes (todas en `C:/Users/Compu casa/auditorias/belleza-app/`):** `AUDITORIA-360-CONSOLIDADA-2026-09-22.md` [A360-CONS], `AUDITORIA-360-2026-09-22.md` [A360], `AUDITORIA-MODULO-PRESTADOR-2026-09-24.md` [PROV], `AUDITORIA-ESCUELA-ACADEMIA-2026-09-22.md` [ESC], `AUDITORIA-ENTREGA-FASE-A-2026-09-24.md` [FA-R1], `AUDITORIA-ENTREGA-FASE-A-RONDA-2-2026-09-24.md` [FA-R2], `AUDITORIA-GRAFO-RAMAS-2026-09-24.md` [GRAFO], `INFORME-PODA-2026-09-24.md` [PODA], `INFORME-REMEDIACION-A360-2026-09-22.md` [REM], `VERIFICACION-INFORME-23-HALLAZGOS-2026-09-22.md` [V23], `POLITICA-RAMAS-BELLEZA-APP-2026-09-24.md` [POL].

**Regla de precedencia aplicada:** cuando un hallazgo aparece en varios documentos, se consolidó en **una fila** y se cita la fuente **más reciente**; los cierres declarados en [REM] y [FA-R2] pesan sobre el diagnóstico de [A360-CONS]/[A360]. Ningún hallazgo de esta tabla es nuevo: todos provienen de las fuentes citadas.

**Convenciones:** ID = prefijo de origen + ordinal (el prefijo agrupa por familia de hallazgo, **la columna `Clase` es la autoritativa**). Clases: DINERO · SEGURIDAD · HONESTIDAD-DE-ESTADO · CONTRATO/API · UX · TÉCNICA. Dueño: `A` = agente ejecutor (Antigravity), `H` = auditor (Hermes), `D` = decisión del dueño (Diego). Estado: `abierta` · `en curso` · `cerrada` · `parcial (ronda N)` · `heredada`. El "ronda N" de `parcial` se refiere a la ronda de entrega/remediación medida, no a una ronda de este documento.

---

## Tabla consolidada

| ID | Defecto (una línea) | Evidencia (archivo:línea o documento+sección) | Clase | Dueño | Criterio de aceptación (test falsable) | Estado |
|---|---|---|---|---|---|---|
| CI-10 | **Ninguna rama nacida de `main` puede producir evidencia de CI**: `main` conserva los marcadores de conflicto en `.github/workflows/ci.yml` ⇒ todo PR desde una rama basada en `main` sale con **0 jobs** (medido: `fix/rls-056-058-cadena` y `docs/sistema-agentes` heredan los 3 marcadores y sus pushes generan runs con 0 jobs). **Y un PR cuya base no sea `main`/`staging` tampoco dispara nada**: `ci.yml` solo escucha `pull_request` contra esas dos ramas (medido: el PR propuesto desde `fix/admin-metricas-sin-datos` con base `fase-a` no genera run) | `git show origin/main:.github/workflows/ci.yml \| grep -cE '^(<<<<<<<\|=======\|>>>>>>>)'` = **3**; `ci.yml` `on.pull_request.branches: [main, staging]` | PROCESO | A | Todo PR que deba aportar evidencia nace de una base con `ci.yml` válido **y apunta a `main`** (o el commit entra en `fase-a`) | abierta |
| CI-11 | **A-02 entregada con pruebas que no prueban**: su test lee `backend/index.js` como texto (`expect(indexContent).toContain(…)` + conteo de `Math.random()`). Medido: reintroduciendo dos meses de ingresos fabricados **sin** `Math.random`, la suite sigue `2 passed, 2 total`. Ningún otro test del repo toca `/api/admin/metrics` ⇒ el endpoint de dinero tiene **cero cobertura de comportamiento** | mutación en `docs/audit/AUDITORIA-ENTREGA-A-02-2026-09-24.md` §2 · rama `fix/admin-metricas-sin-datos` @ `6edd4aff` | PROCESO/TÉCNICA | A | El test ejercita una **función pura** y se pone **rojo** al reintroducir la fabricación (ronda 2, C5'); cero `toContain` sobre el archivo | abierta |
| CI-08 | **La compuerta de secretos quedó ciega al convenio de secretos del proyecto**: la entrega de A-06 añadió `postgres\|dev_\|glowapp_\|default\|root` como prefijos de valor exentos. Medido con mutación: un valor `'glowapp_jwt_production_secure_secret_key_at_least_32_chars'` plantado y `git add`eado ⇒ **`exit 0` (ciega)**, mientras `nvapi-…`, `sk-…`, `ghp_…`, `postgres://u:claveReal@host` y un secreto genérico ⇒ `exit 1` (los ve) | mutación reproducible en `docs/audit/AUDITORIA-ENTREGA-A-06-2026-09-24.md` §2 | SEGURIDAD | A | El prefijo con el que el proyecto nombra sus secretos no puede ser marcador de exención; las mutaciones C1/C2 de la ronda 2 | abierta |
| CI-09 | **El autotest del escáner es tautológico**: el test 1 exige `exit 0` corriendo el script sobre el repo (pasa siempre) y el test 2 compara dos `String.replace` de JavaScript **sin llamar al escáner**; ninguno falla si se quita la normalización. Y **sí** se ejecuta en el paso bloqueante (`jest.config.js:8-11` → `**/tests/**/*.test.js`) ⇒ señal verde falsa en cada run | lectura del test en `5020e4df` + `jest.config.js:8-11` | TÉCNICA | A | Un test que ejerza la lógica del escáner y que **falle** con la normalización quitada (ronda 2, C5) | abierta |
| CI-06 | **El veredicto de la compuerta de secretos depende del fin de línea**: la misma copia del mismo commit da **✅ con `core.autocrlf=true`** y **❌ 39 con `false`** (y en el CI). Medido sobre árboles idénticos (`6d9464ca1abe`, 3.722 archivos trackeados, 225 líneas candidatas): la única diferencia observada es el `\r` final que `git grep` devuelve en una copia y no en la otra. Es un **falso negativo que solo se activa en la copia del dueño** | `docs/audit/AUDITORIA-PR-16-RUN-1681-2026-09-24.md` §3 · run [#1681](https://github.com/Diegoromerov/belleza-app/actions/runs/36072881287) paso 7 `failure`, pasos 8-11 `skipped` | TÉCNICA | A | El mismo commit da el mismo veredicto en un checkout CRLF y en uno LF (reproducido con `git -c core.autocrlf=<v> worktree add …`), con autotest que lo garantice | abierta |
| CI-07 | **La compuerta de secretos no distingue código de prosa**: de sus 39 hallazgos, **25** son `.md`/informes/adjuntos de `.hermes/`, **3** son las credenciales efímeras del propio `ci.yml` (26, 84, 95) y **8** son literales que sí merecen revisión (`config/jwt.js:2`, `biometricCryptoService.js:18,39,59`, `config/database.js:40`, `runMigrations.js:93`, `Dockerfile.postgres:12`, `scripts/generateBaselineR5b.js:72`). El paso bloqueante muere por ruido y deja la suite entera sin correr | reproducción local del paso 7 sobre `c1069e9f` en checkout LF: `❌ 39 · exit 1`; clasificación completa en §2 del informe | TÉCNICA | A | Solo bloquean código y configuración (la prosa va como aviso no bloqueante); los 8 literales con veredicto declarado uno por uno; `ci.yml` exento con motivo escrito al lado | abierta |
| CI-01 | **La compuerta de CI no puede pasar**: `prepareRlsDatabase.js` muere en `058` (`column "tenant_id" does not exist`) y `verifyTenantIsolation.js` no conecta (`password authentication failed for user "app_owner"`) ⇒ el primer run de la historia del repo saldrá rojo **antes** de ejecutar un test | `docs/audit/AUDITORIA-CI-INDEX-GITIGNORE-2026-09-24.md` §A-01 · **re-medido 2026-09-24** sobre base vacía (`glowtest_a01`, PG 16.15): `058` muere con `column "tenant_id" does not exist` (`EXIT=1`), **0 políticas `tenant_isolation*`** creadas y `verifyTenantIsolation.js` no conecta (`app_owner`). Causa nombrada: `services` es la única de las 14 tablas de `058` que existe **sin** la columna (la recibe en `065_multi_tenant_hardening.sql:74`, tres migraciones después) · `058:9-24`/`:39-54` (misma lista) y `:61-63` (`CREATE POLICY`) · `056:6-16` cubre **11** tablas e incluye el nombre inexistente `servicios` | TÉCNICA | A | Sobre base vacía: `prepareRlsDatabase.js` **exit 0** y `verifyTenantIsolation.js` **exit 0**, dos corridas seguidas sin políticas duplicadas; aserción que falle si las listas de tablas de 056 y 058 divergen | **corregida en código** `c36accea` y **verificada por el auditor** (base virgen: `PREPARE ×2 = 0`, `VERIFY = 0`, 12 políticas, 0 duplicadas; aserción probada por mutación: nombra `services`). Condiciones de cierre: rebasar sobre `fase-a` (nace de `main` ⇒ PR con 0 jobs), quitar la línea muerta `servicios` de 056, replicar la aserción en el 2º bucle |
| CI-02 | **El panel de administración fabrica ingresos**: `GET /api/admin/metrics` inventa cinco meses con `Math.random()` y proyecta el mes siguiente por regresión sobre ellos, sin chequeo de entorno ni bandera de degradación (sí lo hace `/api/health`) | `backend/index.js:812-820,840` (`fase-a/verdad-operativa` @ `c1069e9f`) | DINERO | A | Con menos de 3 meses: `history: []`, `projectedRevenue: null` y estado explícito (o 503); con datos reales los números cuadran contra un `SUM` independiente; `Math.random` ausente del camino | abierta |
| CI-03 | **Superficies duplicadas**: 17 montajes repetidos en el mismo prefijo y 5 routers con dos prefijos vivos; `adminPreciosRoutes` montado en `/api/admin/precios` produce `/api/admin/precios/precios` (el router ya incluye `/precios`) | `backend/index.js:388-395,537-541,408-411,418,424,543-560` | CONTRATO/API | A | Un test enumera los montajes y falla si un router aparece dos veces o si una ruta final repite segmento; un prefijo por router | abierta |
| CI-04 | **El bundle de Flutter está congelado por su propia regla**: `.gitignore:68` ignora `backend/public/` mientras 192 archivos siguen versionados ⇒ un rebuild produce archivos que `git add -A` descarta en silencio (es el mecanismo del `NO VERIFICADO` de ARQUITECTURA). Refina TEC-68 | `git check-ignore -v --no-index backend/public/<nuevo>` → `.gitignore:68`; `git ls-files backend/public \| wc -l` = 192 | TÉCNICA | D | Decidir y verificar con comando: artefacto commiteado (quitar la regla + documentar rebuild + compuerta de hash) o derivado (fuera del índice + build en el pipeline) | abierta |
| CI-05 | **Cifras sin procedencia**: el comentario del paso de tests de `ci.yml` afirma «mismas 15 suites rojas heredadas, 0 fallos nuevos» sin origen declarado, y el paso bloqueante corre ≈66 de las 80 suites (el patrón `authRoutes` hoy no excluye nada) | `.github/workflows/ci.yml` (paso «Run Backend Integration & Unit Tests») · inventario medido: 80 `.test.js` fuera de `node_modules`, 14 casan patrones excluidos | TÉCNICA | A | El número de suites rojas sale del run (URL) o de una corrida declarada con su comando; los patrones de exclusión que no excluyen nada se corrigen o se eliminan | bloqueada por CI-01 |
| DIN-01 | No existe cobro real: `payBooking` devuelve 501 en producción, no hay ninguna llamada a Wompi en el repo y la reserva queda en `PENDIENTE_PAGO` para siempre | PROV §C-02 (etapa 6 de §1): `bookingController.js:443-468`; `grep` de `https?://` en `wompiService.js` = 0 | DINERO | D | Con `NODE_ENV=production` el pago responde 501 de forma comprobable y existe creación de transacción Wompi (`grep -c 'api.wompi.co'` > 0) o el cobro se retira del producto | abierta |
| DIN-02 | La app recolecta número de tarjeta, vencimiento, CVV y titular y no los envía a ningún sitio (el POST solo lleva `{payment_method}`): pasarela simulada con apariencia de real y riesgo de cumplimiento PCI | PROV §C-02 (`api_service.dart:385-398`; `wompi_payment_sheet.dart:611-684`) · FA-R2 §B4 (`:661`, `:698`) | DINERO | A | Con `itemType == 'store'` no se montan `_cardCtrl`/`_cvvCtrl`; una prueba de UI falla si existe campo de tarjeta sin pasarela cableada | abierta |
| DIN-03 | El wallet se puede acreditar sin que nadie pague: `PATCH /bookings/:id/status` acepta cualquier transición, `complete` no mira `payment_status` y `confirm-otp` escribe `paid` y acredita `pago_neto_prestador` | PROV §C-01 (`bookingController.js:309-323`; `paymentRoutes.js:118,192-211,284-299`) | DINERO | A | La cadena `PATCH status → EN_PROGRESO → complete → confirm-otp` devuelve 409 `BOOKING_NOT_PAID` y **no** escribe en `provider_wallet` | abierta |
| DIN-04 | Transiciones de estado sin máquina de estados: caben `PENDIENTE_PAGO→COMPLETADA` y `CONFIRMADA→PENDIENTE_PAGO` sin tocar `payment_status` ni auditar; habilita reseñas sobre citas no cobradas y KPIs inflados | PROV §A-08 (`bookingController.js:292-339`, `:756`) | DINERO | A | Los 6 saltos inválidos dan 409 con estado actual y permitidos; el prestador solo puede `CONFIRMADA→EN_PROGRESO` y `EN_PROGRESO→FINALIZADA_PRESTADOR` | abierta |
| DIN-05 | Disputas con tres implementaciones paralelas y vocabularios distintos; resolver por `adminRoutes`/`disputeController` deja el dinero congelado en `saldo_en_disputa` para siempre | PROV §A-04 (`paymentRoutes.js:891-1003`; `disputeController.js:6-81`; `adminRoutes.js:34-82`) | DINERO | A | `UPDATE disputas` aparece en un solo módulo y ningún endpoint resuelve sin mover `provider_wallet` | abierta |
| DIN-06 | Congelar con `GREATEST(0, …)` permite pagar dos veces: recorta el descuento a 0 pero acredita `saldo_en_disputa` igual y al resolver `FAVOR_PRESTADOR` se re-libera | PROV §A-05 (`paymentRoutes.js:960-977`, `:1140-1147`) | DINERO | A | Sin fondos el congelamiento da 409 y no acredita `saldo_en_disputa`; invariante `pendiente+disponible+en_disputa` verificada antes/después | abierta |
| DIN-07 | Dos dueños del mismo saldo: `GET /api/wallet` madura saldos en una ruta de lectura sin transacción ni `FOR UPDATE`, y el job `madurarSaldosPendientes` repite la operación | PROV §A-06 (`paymentRoutes.js:405-433`; `paymentJobs.js:40-91`) | DINERO | A | Dos `GET /api/wallet` concurrentes (`Promise.all`) no duplican `saldo_disponible`; solo el job madura | abierta |
| DIN-08 | Retiro: debita `saldo_disponible` e inserta `retiros(PROCESANDO)` antes de que `crearPayout` (siempre rechazado en prod) confirme; sin reintento ni cola, el saldo queda descontado | PROV §A-01 (`paymentRoutes.js:781-825`; `wompiService.js:9-17,82`; `paymentJobs.js:235-237`) | DINERO | A | Ninguna `saldo_disponible` baja sin `retiros.referencia_wompi` confirmada, o el retiro queda en estado reintentable con job | abierta |
| DIN-09 | La comisión real (curva continua 15–28% `+ 8% fijo` en `impuestos_estado`) no es la que la app comunica: "Descuento Plataforma (20% total)" hardcodeado; el prestador retiene 59–71% y la UI dice 80% | PROV §A-07 (`migrations/011_update_commission_trigger.sql`; `provider_dashboard_screen.dart:354-374`; `admin-glow/admin.model.js:142-145`) | DINERO | A | La UI muestra `comision_plataforma`/`impuestos_estado`/`pago_neto_prestador` reales; `grep "0\.20"` en `provider_dashboard_screen.dart` = 0 y desaparece la sub-línea del "8% asumido en tu beneficio" | abierta |
| DIN-10 | `platform_config.comision_plataforma_pct = 20` no lo lee nadie, y `cancelacion_libre_horas`, `disputa_max_reembolso_pct` y `riesgo_suspender_score` tampoco: config que promete comportamiento inexistente | PROV §A-07 y §A-09 (`bookingController.js:397-435`) | DINERO | A | Cada campo de `platform_config` es leído por código con test, o se retira; `cancelBooking` aplica o rechaza según las horas | abierta |
| DIN-11 | El webhook de pago no cumple el contrato de Wompi (valida HMAC del cuerpo con header propio; Wompi usa `signature.properties`+`timestamp`+secreto en `X-Event-Checksum`) y `req.rawBody` no lo produce nadie | PROV §M-08 (`bookingController.js:9-39`) | DINERO | A | Firma de un evento de ejemplo de la documentación validada y evento duplicado ignorado (hoy rechazaría con 401 todos los legítimos) | abierta |
| DIN-12 | `disbursePayout` es código muerto y, de ejecutarse, sobrescribiría la fila de `transactions` del cobro con `payment_method='NEQUI'` (una sola fila por `booking_id`) | PROV §B-05 (`wompiService.js:26`, `:44-55`) | DINERO | A | La función se elimina o `transactions` deja de tener una fila única por `booking_id` con test que lo pruebe | abierta |
| DIN-13 | El simulador de cobro estaba activo e incondicional en 4 sitios (pago de cita en backend, tienda Flutter, propina y consola de admin) | A360-CONS §C-05 · cierre en REM §2 (C-05) | DINERO | H | `payBooking`, `crearPayout` y `payMedicalValidation` devuelven 501/rechazo sin `ALLOW_PAYMENT_SIMULATOR`; `src/tests/audit360-remediation.test.js` 21/21 verde | cerrada |
| DIN-14 | GlowShop dejó de simular éxito pero no está cableado a `POST /api/store/checkout` (que existe): falta el helper en `ApiService` y el pedido no se persiste con cobro | REM §3.1 (`productRoutes.js:38`) | DINERO | D | Un pedido de tienda solo pasa a pagado con transacción real, o la tienda aparece deshabilitada de forma explícita en producto | abierta |
| DIN-15 | El OTP que libera el pago no se entrega a nadie: se guarda solo el hash y la respuesta afirma "Se envió el código al cliente" sin enviarlo; el único canal posible (`emailService.js`) es un `console.log` con el bloque de nodemailer comentado | PROV §C-03 (`paymentRoutes.js:140-165`; `emailService.js:14-24`; `otp_confirm_screen.dart:206-215`; `provider_dashboard_screen.dart:914-921`) | DINERO | A | Un cliente real recibe el código por un canal transaccional antes de que `complete` responda 200, con test de entrega; sin canal, el endpoint devuelve error explícito | abierta |
| SEG-01 | WebSocket sin autenticación: sin `verifyClient`, registro con solo enviar `userId`, `join_booking_room` sin verificar pertenencia y escritura de ubicación de cualquier prestador | A360-CONS §C-04 (`websocketService.js:60-107`) · cierre en REM §2 (C-04) | SEGURIDAD | H | Abrir un WS sin token recibe error; `providerId` se deriva del token; test de pertenencia a la reserva | cerrada |
| SEG-02 | RLS inerte en su origen: `ENABLE` sin `FORCE`, contexto de tenant en conexión arbitraria del pool (`set_config(…,false)`), `tenantContext.js` con un segundo `Pool` nunca montado y 6 tablas sin RLS | A360-CONS §C-09 · PROV §M-02 · REM §3.4 | SEGURIDAD | A | Ningún cambio. Pendiente: medir `relforcerowsecurity` en producción y montar `tenantContextMiddleware` o borrar `tenantContext.js` | parcial (ronda 2) |
| SEG-03 | El backfill de tenant asigna todo el histórico a un único tenant `demo`: el aislamiento multi-tenant es nominal y las suites `tenant-isolation` no describen los datos | A360-CONS §C-10 · `backend/migrations/manual/070_reassign_tenant_id_by_owner.sql` (REM §3.2) | SEGURIDAD | D | `SELECT tenant_id, count(DISTINCT owner) FROM usuarios GROUP BY 1` sin ningún tenant con más de un dueño; ventana de aplicación aprobada por el dueño | parcial (ronda 2) |
| SEG-04 | Secretos de producción siguen en el historial de git aunque el archivo salió del índice: `JWT_SECRET`, `ENCRYPTION_KEY`, `DATABASE_URL`, `GEMINI/YOCAM/OPENUV_API_KEY` y clave NVIDIA | REM §3.3 · A360-CONS §C-02 | SEGURIDAD | D | `git log -p --all -- backend/.env.production \| wc -c` = 0 y los 7+1 secretos rotados en su proveedor | parcial (ronda 2) |
| SEG-05 | Contraseñas de usuario y claves de API en los logs (`authController.js:87` imprime email y password; `seed_glowapp_kb.js:7` la clave NVIDIA; `paymentRoutes.js:157` el OTP) | A360-CONS §C-06 · cierre en REM §2 (C-06) | SEGURIDAD | H | `grep -rn "Password:" backend/src` = 0 y un test falla si un `console.*` interpola `password`/`token`/`otp`/claves | cerrada |
| SEG-06 | La clave AES de datos biométricos se derivaba del `JWT_SECRET` (que está commiteado) y continuaba en vez de fallar | A360-CONS §C-11 · V23 #4 (cerrado) · REM §2 (C-11) | SEGURIDAD | H | `biometricCryptoService.js:33` lanza si se intenta derivar de `JWT_SECRET`; solo conserva la derivación legada para descifrar datos antiguos | cerrada |
| SEG-07 | Fuga cross-usuario del caché semántico de Aura: `generateCacheKey` hashea solo el embedding y el hit (similitud ≥ 0,92) se inserta en el hilo del usuario que consulta | A360-CONS §C-12 · cierre en REM §2 (C-12) | SEGURIDAD | H | La clave y el índice de candidatos incluyen `user_id`/`tenant_id`; test de dos usuarios con la misma consulta no devuelve la respuesta del otro | cerrada |
| SEG-08 | La blacklist de tokens revocados es FAIL-OPEN: si Redis cae, `auth.js:16` cae al `catch` que solo loguea y los tokens revocados vuelven a ser válidos (el comentario dice "FAIL-SAFE") | V23 #19 (`auth.js:16`) | SEGURIDAD | A | Con Redis caído un token revocado recibe 401 (fail-closed) o el proceso no arranca; test con `redisClient` no `isReady` | abierta |
| SEG-09 | 94 handlers `async` en `src/routes/*.js` frente a 2 con wrapper: con Express 4 un rechazo async no capturado mata el proceso (Node ≥ 15) | V23 #9 (solo academia corregida con `src/utils/expressAsync.js`) | SEGURIDAD | A | Los 94 handlers pasan por `expressAsync`; test que provoca un throw async y comprueba 500 sin caída del proceso | abierta |
| SEG-10 | CSP anulada: `connectSrc` e `imgSrc` con `*`, `scriptSrc` con `unsafe-inline` y `unsafe-eval` | A360-CONS §A-04 (`index.js:222,223-227,251`) | SEGURIDAD | A | Lista explícita de dominios, sin `*` ni `unsafe-eval`; test de cabeceras sobre `/api` | abierta |
| SEG-11 | TLS sin verificar en llamadas salientes. **Incondicional** (`rejectUnauthorized: false` fijo): `index.js:278` (tiles), `config/config.js:13,26`, `knexfile.js:8`, `runMigrations.js:84`. **Condicional** (solo desactiva con `DB_SSL_REJECT_UNAUTHORIZED='false'`): `src/config/db.js:21`, `src/config/database.js:31`. Correcto y ejemplar: `services/biometric/youcam.client.js:9` (`true` en producción). **Citas corregidas el 2026-09-24 tras verificación directa**: la fila heredada citaba `db.js:23` y `db.js:667`, que hoy **no** contienen `rejectUnauthorized` | A360-CONS §A-05 · V23 #3 · **re-verificado** con `grep -rn "rejectUnauthorized" backend/ --include=*.js` (8 apariciones, 5 incondicionales) | SEGURIDAD | A | Los 5 sitios incondicionales pasan a `true` o CA bundle; test de conexión que falla con certificado inválido | abierta |
| SEG-12 | `PUT/GET /api/bookings/client` no devuelve `provider_id`: el seguimiento abre el chat del literal `'2'` y "Reprogramar" pasa `null` a `/provider-detail` | PROV §M-03 (`bookingController.js:346-361`; `booking_tracking_screen.dart:158,353-354`; `client_bookings_screen.dart:1069-1073`) | CONTRATO/API | A | `/api/bookings/client` devuelve `b.provider_id` y el fallback `'2'` desaparece del código | abierta |
| SEG-13 | El PIN de 4 dígitos del cliente no es un control: se genera con `Math.random()`, se guarda en claro y el servidor nunca lo valida | PROV §M-04 (`bookingController.js:148`; `booking_tracking_screen.dart:157`) | SEGURIDAD | A | O se elimina de la UI o el servidor lo valida contra `bookings.pin_verificacion` (nunca en claro) | abierta |
| SEG-14 | `requireAdmin` acepta identidad privilegiada por cadena literal (`admin@beautyapp.com` o `admin`) además del rol | PROV §M-06 (`paymentRoutes.js:48-55`) | SEGURIDAD | A | El guard solo acepta rol desde el token verificado; test con email literal recibe 403 | abierta |
| SEG-15 | `ALLOW_MEMORY_FALLBACK` sin guarda de `NODE_ENV`: una variable mal puesta pone toda la API a servir memoria y, combinada con los usuarios demo de contraseña conocida, actúa como backdoor de autenticación | V23 #12/#1 (`db.js:695`) | SEGURIDAD | A | `memoryFallbackAllowed()` exige `NODE_ENV !== 'production'`; test con `ALLOW_MEMORY_FALLBACK=true` y `NODE_ENV=production` arranca en fallo | abierta |
| SEG-16 | CORS del worker IA con `allow_origins=['*']` + `allow_credentials=True` (combinación que el navegador rechaza) | A360-CONS §R-09 (`ai_worker/main.py`) | SEGURIDAD | A | El worker valida contra lista blanca y un test de preflight no devuelve `*` con credenciales | abierta |
| SEG-17 | `startup/app.js` (código muerto) contiene un CORS que acepta cualquier `*.up.railway.app` y un agente TLS sin verificación: si algún día se monta, entra un agujero | V23 "Lo que el informe NO vio" #2 (`app.js:98-107,120,163`) | SEGURIDAD | A | El archivo se borra; `grep -rn "up.railway.app" backend/src backend/index.js` = 0 | abierta |
| SEG-18 | `POST /api/ai/orchestrate` sin autenticación, con herramientas decididas por el modelo y guarda de traversal decorativa que resuelve a la raíz del sistema | A360-CONS §A-17 · cierre en REM §2 (A-17) | SEGURIDAD | H | `authMiddleware`+admin en la ruta y guard que acota al repo con `path.relative`; invocación anónima recibe 401 | cerrada |
| SEG-19 | Las notificaciones push son simuladas devolviendo `success: true`, así que el producto cree que notifica sin que nada llegue | A360-CONS §A-08 (`fcmNotificationService.js:86`) | HONESTIDAD-DE-ESTADO | A | La función devuelve `success: false, reason:'FCM_NOT_CONFIGURED'` y el motivo se ve en el panel | abierta |
| SEG-20 | Superficie de diagnóstico expuesta sin auth (`/status`, `/api-docs`) y `/api/health` que escribía con `setval` en cada probe y respondía 200 con la BD caída | A360-CONS §A-06 · REM §2 (C-03) y FA-R1 (health 503 DEGRADED) | SEGURIDAD | A | `/status` y `/api-docs` exigen admin en producción; `/health` no muta la BD (verificado: `servingFabricatedData` en 503) | parcial (ronda 2) |
| SEG-21 | `GET /uploads` sirve todo desde disco local sin autenticación y, en EKS con más de una réplica, el disco no se comparte (enlaces rotos) | A360-CONS §A-07 (`index.js:108-145,349`) | SEGURIDAD | A | Subidas a object storage con URLs firmadas servidas por endpoint autenticado; test de acceso anónimo recibe 401 | abierta |
| SEG-22 | El catch-all `GET /api/providers` silenciaba errores y devolvía 200 con datos fabricados (7 prestadores inexistentes y distancias inventadas) | FA-R1 §H-03 · cierre en FA-R2 (C1) | HONESTIDAD-DE-ESTADO | H | Con `DATABASE_URL` inalcanzable, `curl /api/providers` → 503 + `X-GlowApp-Degraded` + `{"error":"PROVIDER_SEARCH_DEGRADED"}` | cerrada |
| SEG-23 | Doble verificación de pago incompatible: la app del prestador pide el `pin_verificacion` de 4 dígitos y lo envía en el body, pero el backend **no lee el body** y genera otro código de 6 dígitos: el prestador cree verificar la entrega y el sistema no lo registra | PROV §C-04 (`api_service.dart:825-852`; `provider_dashboard_screen.dart:3034-3067`; `paymentRoutes.js:104-176`; `bookingController.js:148`) | SEGURIDAD | D | Queda **una** verificación y se valida en servidor; test: el prestador no puede completar con el PIN de 4 dígitos y el servidor rechaza cualquier código no emitido | abierta |
| HON-01 | `/api/products` responde 200 con datos fabricados mientras el servidor está en modo degradado: ninguna superficie debería dar 2xx si su consulta falló | FA-R2 §B3 y §S1 (`GET /api/products` → 200, `X-GlowApp-Degraded: memory-fallback`, 36 bytes) | HONESTIDAD-DE-ESTADO | A | Con `servingFabricatedData === true`, `/api/products` no responde 2xx; el smoke lo detecta y sale ≠ 0 | abierta |
| HON-02 | El guardián de la fase aprueba un sistema que miente: `npm run smoke:surfaces` sale 0 (EXIT=0) mientras `/api/products` responde 200 degradado; `faked_success` solo evalúa 2 rutas hardcodeadas, `wrote_to_db` está hardcodeado a `false`, `empty_like` se calcula y nunca decide, y no escribe informe | FA-R2 §B3 (`backend/scripts/smokeSurfaces.js:56,72,78-87,100`) | HONESTIDAD-DE-ESTADO | A | Con `/api/products` degradado, `npm run smoke:surfaces` sale ≠ 0 y escribe `docs/audit/smoke-*.json` (par RED→GREEN como evidencia) | abierta |
| HON-03 | El runner de migraciones reporta "Migración 001…072 aplicada exitosamente" sobre una base inalcanzable mientras el pool avisa `ECONNREFUSED` | FA-R1 §H-10 | HONESTIDAD-DE-ESTADO | H | Con `DATABASE_URL` en `127.0.0.1:59999` el arranque falla en vez de imprimir éxitos; falta confirmar si usa `DATABASE_URL_ADMIN` o el pool | abierta |
| HON-04 | `testConnection()` mentía y el fail-open dejaba el proceso sirviendo fixtures en memoria de forma permanente y casi invisible (`db.js` 24 ramas con filas inventadas) | A360-CONS §C-03 · cierre de núcleo en REM §2 (C-03) | HONESTIDAD-DE-ESTADO | H | Sin cortocircuito a `handleMemoryQuery`, `isPgAvailable` recuperable y `/api/health` expone `servingFabricatedData` (medido en 503 DEGRADED) | cerrada |
| HON-05 | `memoryFallbackAllowed` es `false` mientras el proceso **está** sirviendo memoria: los dos indicadores de `/api/health` se contradicen | FA-R1 "Deuda heredada" (mismo `/api/health` degradado) | HONESTIDAD-DE-ESTADO | A | Los dos indicadores coinciden siempre; test de arranque degradado | abierta |
| HON-06 | El diagnóstico biométrico era inventado: constantes `hidratacion: 85.5`/`sebo: 40.2` para toda imagen y un `except` que devolvía el mismo diagnóstico ante imagen corrupta con HTTP 200 | A360-CONS §C-08 · cierre en REM §2 (C-07/C-08) | HONESTIDAD-DE-ESTADO | H | Métricas reales por imagen (75.0 vs 89.0) y 422/400/413 en vez de 200 sintético; `python ai_worker/tests/test_beauty_scan_contract.py` 7/7 | cerrada |
| HON-07 | Mocks biométricos en el frontend: ante cualquier no-2xx `biometric_service.dart` devuelve `getMockBiometricJson()` y `processing_screen.dart` navega a resultados con "Generando diagnóstico de demostración" | A360-CONS §C-08 (`biometric_service.dart:110-116`; `processing_screen.dart:88-98`) | HONESTIDAD-DE-ESTADO | A | Ningún mock se devuelve sin flag de demo tipado visible (`isMock: true`); test de error de red no produce diagnóstico | abierta |
| HON-08 | `simulateDoctorReview` escribía a los 15 s una nota clínica aleatoria en `validaciones_medicas` y marcaba `estado='revisado'` | A360-CONS §C-08 y C-10 · cierre en REM §2 (C-10) | HONESTIDAD-DE-ESTADO | H | La función está eliminada de `designsController`; el estado permanece `pendiente` sin profesional real | cerrada |
| HON-09 | El fallback de Aura (`safe_fallback`) se envía y se persiste como respuesta clínica, con la misma apariencia que una respuesta real | A360-CONS §C-13 (`geminiService.js:880-908`, `INSERT INTO messages` 891-896) | HONESTIDAD-DE-ESTADO | A | Si los LLM fallan el usuario recibe indisponibilidad explícita, no persistida como respuesta clínica, y `llm_used` se expone | abierta |
| HON-10 | Análisis facial "simulado" devuelto como resultado si falta `GEMINI_API_KEY`, con referencias de validación inventadas en proceso | A360-CONS §R-05 (`designsController.js:434,565,1582`) | HONESTIDAD-DE-ESTADO | A | Sin `GEMINI_API_KEY` el endpoint devuelve error explícito (no resultado simulado) con test | abierta |
| HON-11 | Respuestas de benchmark y `schema` simuladas servidas como datos del orquestador | A360-CONS §R-06 (`orchestrator.service.js:40,48`) | HONESTIDAD-DE-ESTADO | A | Las respuestas simuladas llevan marca visible o se eliminan; test que las detecta | abierta |
| HON-12 | Texto nuevo de A1.T5 afirma un hecho que el sistema no ejecuta ("El pago se procesa directamente en el establecimiento", "sin recolectar credenciales bancarias") mientras `payBooking` da 501 | FA-R2 §B4 | HONESTIDAD-DE-ESTADO | A | El texto elimina toda afirmación de cobro no implementada; revisión de copy contra el flujo real | abierta |
| HON-13 | La app muestra "Descuento Plataforma (20% total)" con el 20% hardcodeado y una sub-línea que afirma que la plataforma asume el 8% "en tu beneficio" cuando se le descuenta al prestador | PROV §A-07 (`provider_dashboard_screen.dart:354-374`) | HONESTIDAD-DE-ESTADO | A | Ver DIN-09: desglose real desde `/bookings/provider` y sin literales | abierta |
| API-01 | Membresías rotas por columna inexistente: el middleware consulta `business_profile_id`, que no existe en la tabla real (tiene `establishment_id`); `013_memberships.sql` nunca se aplica porque los runners solo toman `backend/migrations/*.sql` | PROV §C-05 (`membership.middleware.js:21-31`; `models/Membership.js:18-22`; consulta literal `SELECT "Membership"."business_profile_id" FROM memberships` → `ERROR: column does not exist`) | CONTRATO/API | A | `GET /api/services/provider` responde 200 para los 5 prestadores con membresías múltiples (ids 7, 11, 74, 104, 106) | abierta |
| API-02 | `/api/v1/beauty-scan` llamaba a una ruta que no existe en `ai_worker` (solo hay `/api/v1/analyze-skin` y `/v1/ai/consult`) y el contrato no cuadraba (multipart de 4 fotos vs JSON `image_base64`) | A360-CONS §C-07 · cierre en REM §2 (C-07/C-08) | CONTRATO/API | H | Contrato unificado contra `openapi.json` del worker; test de contrato 7/7 verde | cerrada |
| API-03 | Esquema no versionado: `salones`, `salon_miembros`, `salon_invitaciones`, `providers`, `rag_chunks`, `aura_knowledge_chunks` sin `CREATE TABLE` en el repo; `memberships`/`business_profiles` solo en `src/db/migrations/012,013`, que el runner nunca lee | A360-CONS §A-16 · PROV §C-05 (`establishment_id` no aparece en ningún archivo del repo) | CONTRATO/API | A | `pg_dump --schema-only` de producción como base y migraciones versionadas para todo lo que el código consulta; un entorno nuevo levanta solo desde el repo | abierta |
| API-04 | Cuatro navegaciones Flutter a rutas no declaradas (`/wallet`, `/settings`, `/glowaipremium`, `/provider-detail`) provocan "Could not find a generator for route" | A360-CONS §F-01 (`my_glow_dashboard_screen.dart:171,178`; `wardrobe_dashboard_screen.dart:204`; `client_bookings_screen.dart:1085`) | CONTRATO/API | A | Las 4 rutas existen en `main.dart` o las llamadas desaparecen; `flutter analyze` sin `undefined_route` | abierta |
| API-05 | `salonId ?? 1`: el OWNER invita a un miembro al salón 1 (otro tenant) si los datos aún no cargaron | A360-CONS §F-02 (`salon_dashboard_screen.dart:307`) | CONTRATO/API | A | Sin `salonId` la app bloquea la acción; test de widget con datos incompletos no emite invitación al salón 1 | abierta |
| API-06 | El contexto SaaS validado por membresía no se usa para el scoping: `businessController` consulta por `req.user.id`/`tenant_id` e ignora `req.user.businessProfileId` | A360-CONS §A-02 (`membership.middleware.js:82-84` vs `businessController.js:45-46,74-75,95-96,113-114`) | CONTRATO/API | A | Test con dos membresías: el recurso devuelto pertenece al contexto seleccionado | abierta |
| API-07 | `GET /api/bookings` no existe (solo `/bookings/provider` y `/bookings/client`) y el panel lo muestra como "sin citas" porque el error de `useBookings` nunca se renderiza | A360-CONS §D-02 y §D-03 (`api-client.ts:43`; `bookingRoutes.js:9,12,15`; `useBookings.ts:15,23-25`) | CONTRATO/API | A | `/api/bookings` existe o el panel muestra el error real; los 4 consumidores renderizan el fallo | abierta |
| API-08 | Cuatro endpoints de chat que el panel usa no existen (`/api/chats*`) y la pantalla responde con `setTimeout` | A360-CONS §D-07 (`api-client.ts:60,65,70` vs `chatRoutes.js:10-13`) | CONTRATO/API | A | Los endpoints existen con test, o la pantalla se retira; `grep setTimeout` en la pantalla = 0 | abierta |
| API-09 | `PUT /api/users/profile` cuando el backend solo acepta `PATCH`, y el submit del perfil no llama a ninguna API | A360-CONS §D-08 (`api-client.ts:76` vs `index.js:1138`; `perfil/page.tsx:9-17`) | CONTRATO/API | A | Método unificado y el submit invoca la API (test de integración del cliente) | abierta |
| API-10 | El chat de Aura nunca propaga `tenantId` (conocimiento propio inalcanzable) y el filtro `category` que envía el ejecutor no existe en el receptor (se descarta en silencio) | A360-CONS §A-21 (`geminiService.js:297,532,829`; `auraToolExecutor.js:277`; `ragService.js:86`) | CONTRATO/API | A | Test de retrieval con tenant propio devuelve chunks de ese tenant; `category` aceptado o eliminado del emisor | abierta |
| API-11 | En el fallback de Gemini los nombres de los argumentos de las herramientas no coinciden con los que lee el ejecutor: herramientas clave se invocan con `undefined` | A360-CONS §A-22 (`geminiService.js:636-697` vs `auraToolExecutor.js:236-272`) | CONTRATO/API | A | Test que valida los nombres de argumentos de cada herramienta en ambos caminos | abierta |
| API-12 | `skin_metrics.py` importa `SkinMetricsResponse`, que no existe en `models.py`, y el módulo no se importa desde `main.py`: capacidad aparente e inutilizable | A360-CONS §R-08 (`ai_worker/services/skin_metrics.py:3,7`) | CONTRATO/API | A | El módulo importa y responde en un test, o se elimina | abierta |
| API-13 | La cadena de expedición de certificados no exige curso completo: con 2 llamadas HTTP se obtiene el certificado; `POST /lessons/:id/complete` acepta cualquier UUID y un UUID malformado da 500 en vez de 400 | ESC §B3 (`academyRoutes.js:222-282`, `:94-109`) | CONTRATO/API | A | `submit-quiz` devuelve 409 si el curso no está 100% completo; lección ajena y UUID inválido dan 400/403 con test | abierta |
| API-14 | La API del alumno no distingue rol: un `CLIENTE` autenticado puede emitirse certificados de prestadora | ESC §B4 (`academyRoutes.js` solo usa `authMiddleware`; `008:29,68`) | CONTRATO/API | A | `submit-quiz` valida rol prestador/salón; test con rol CLIENTE recibe 403 | abierta |
| API-15 | `POST /worksheets/submit` y `/ai-discrepancy/log` no validan que `lessonId` pertenezca al curso del alumno ni deduplican | ESC §B20 (`academyRoutes.js:154-198`) | CONTRATO/API | A | Lección/curso ajenos dan 403; el log de discrepancias deduplica con test | abierta |
| API-16 | Gamificación rota: `xpLogController.js` consulta/inserta `xp_logs(points, description)` pero la tabla define `xp_amount/reason/metadata` → 500 en `/api/xp-logs`; `learningPathRoutes.js:8` requiere un modelo inexistente | ESC §B15 (`xpLogController.js:42,59`; `023:27-34`; `index.js:409`) | CONTRATO/API | A | `/api/xp-logs` responde 200 con test, o la ruta se retira; `learningPath*` se borra o se conecta | abierta |
| API-17 | Los "tests de integración" del módulo prestador no tocan el módulo: `booking.test.js` levanta su propia app con handlers inline y `payment.test.js` prueba una firma local que no es la de producción | PROV §M-09 (`tests/booking.test.js:6-43`; `tests/payment.test.js:10-30`) | TÉCNICA | A | Los tests importan `bookingController` y la firma de producción; correrlos con el controlador roto debe ponerse rojo | abierta |
| API-18 | Un prestador nuevo es invisible y no hay flujo de aprobación: `GET /api/providers` exige `APROBADO` + `ubicacion`, el onboarding crea `PENDIENTE` (9 perfiles para 41 cuentas, 4 `PENDIENTE`) y los botones de aprobación del panel no tienen `onClick` | PROV §M-01 (`providerController.js:68-69`; `authController.js:329`; `prestador/page.tsx:115-120`) | UX | A | Aprobar desde el panel cambia `estatus_verificacion` y el perfil aparece en `/api/providers` (test end-to-end) | abierta |
| UX-01 | Siete tareas de la Fase A no existen o quedaron parciales, incluidas las tres del guardián RED→GREEN (`smokeSurfaces.js`, `listRoutes.js`, `auditFakeSuccess.js`, informes `docs/audit/*.json`) | FA-R1 §H-04 · FA-R2 (A3.T1/T2/T3 existen pero el guardián da verde mientras el sistema miente) | UX | A | Las tareas existen con su par RED→GREEN como evidencia principal | parcial (ronda 2) |
| UX-02 | A1.T4 (barrido de `catch` que fingen) sin línea base: el informe se generó después de C1 y ya no detecta el caso conocido; cobertura no declarada (solo `src/controllers` + `src/routes`) | FA-R2 §B5 (`auditFakeSuccess.js:67-68`) | UX | A | Línea base anterior al fix y cobertura declarada incluyendo services, jobs y middleware | abierta |
| UX-03 | El artefacto declara `wrote_to_db: false` hardcodeado y no escribe informe: métrica fabricada dentro del guardián antifabricación | FA-R2 §B3 (`smokeSurfaces.js:100`) | HONESTIDAD-DE-ESTADO | A | `wrote_to_db` se deriva de una comprobación real y el informe JSON se escribe en `docs/audit/` | abierta |
| UX-04 | La entrega amplió la app sin pedirlo: `index.js` ganó 25 mounts en un commit sobre otra cosa (17 duplicados muertos e inalcanzables + 8 caminos base nuevos sin prueba ni declaración) | FA-R2 §B2 (commit `5f834289`) | TÉCNICA | A | Diff de `index.js` sin mounts nuevos no pedidos; los 8 alias con `curl` sin token y cobertura declarada | abierta |
| UX-05 | El PR de la rama `fase-a/verdad-operativa` no existe: la rama está en GitHub (5 commits, `cfec993a`) pero la API devuelve `[]` | FA-R2 §B1 · PODA §6.1 | TÉCNICA | D | URL de PR real con run de CI visible | abierta |
| UX-06 | La entrega de Fase A no fue entregable en ronda 1: todo sin commitear, rama inexistente en el remoto, sin PR y con un marcador de conflicto en el archivo *staged* (`checkNoConflictMarkers.js:6`) | FA-R1 §H-01 y §H-02 · cierre en FA-R2 (B1 resuelto, árbol limpio) | TÉCNICA | H | `git show 345dfeb9:backend/scripts/checkNoConflictMarkers.js \| grep -c '^<<<<<<<'` = 0 y árbol limpio | cerrada |
| UX-07 | El barrido declara un arreglo que no cambió el comportamiento: `/api/providers` seguía devolviendo 200 con 7 prestadores inexistentes y distancias inventadas | FA-R1 §H-03 · cierre en FA-R2 (C1: 503 + `X-GlowApp-Degraded`) | HONESTIDAD-DE-ESTADO | H | `curl /api/providers` con base inalcanzable → 503 `PROVIDER_SEARCH_DEGRADED` | cerrada |
| UX-08 | El `catch` de `providerController` exponía `error.message` al cliente en la única ruta pública de la fase | FA-R1 §H-06 · cierre en FA-R2 (`500 {success:false,error:'INTERNAL_SERVER_ERROR'}` sin `message`) | SEGURIDAD | H | El cuerpo de error no incluye `error.message` | cerrada |
| UX-09 | El fallback geográfico sigue devolviendo lat/lon fijos y `distance_meters` calculados sin marca `degraded: true` | FA-R1 §H-07 (`providerController.js:117-139`) | HONESTIDAD-DE-ESTADO | A | El fallback marca `degraded: true` en todas sus respuestas (test) o se elimina | abierta |
| UX-10 | El seguimiento "en vivo" del prestador es interpolación lineal local: llega "siempre" en ~40 s sin GPS, y la ruta se simula ~20 s habilitando el botón de llegada por tiempo, no por geocerca | PROV §B-04 (`booking_tracking_screen.dart:125-148`) · A360-CONS §F-03/F-04 (`provider_route_screen.dart:45,60-88`) | UX | A | El estado de llegada depende de geocerca real o la UI lo declara simulado; test de seguimiento sin GPS no afirma llegada | abierta |
| UX-11 | Generador de notificaciones falsas cada 45 s (ofertas y nombres concretos), con timer que no se cancela y `navigatorKey` distinto al de `MaterialApp` (nunca se ven y consumen batería) | A360-CONS §F-05 (`notification_service.dart:9-11,24-34,49-53` vs `main.dart:163`) | UX | A | El timer se cancela y las notificaciones usan el `navigatorKey` real, o el servicio se elimina | abierta |
| UX-12 | El desglose de pagos del prestador muestra cuenta Nequi y referencia Wompi inventadas (campo que el backend no devuelve) | A360-CONS §F-06 (`provider_dashboard_screen.dart:296-300` vs `bookingController.js:266-283`) | UX | A | El desglose usa solo campos devueltos por el backend; test de payload sin referencia no pinta referencia | abierta |
| UX-13 | El prestador no se entera de la reserva: `notifyProviderNewBooking` solo emite por WebSocket abierto y el "SMS" es `console.log`; no hay push, email ni bandeja persistente | PROV §A-03 (`websocketService.js:159-175`; `bookingController.js:211-216`) | UX | A | Aviso persistente (bandeja + push si hay canal); una cita creada con la app cerrada aparece al abrir | abierta |
| UX-14 | El botón de escape del prestador está muerto: "liberación manual/reportar caso" llama `EN_DISPUTA`, que no está en `validStatuses` → 400 "Estado inválido" siempre | PROV §A-02 (`provider_dashboard_screen.dart:833-834`; `bookingController.js:309-312`) | UX | A | El diálogo completa el flujo o `EN_DISPUTA` se añade con endpoint propio; test del camino | abierta |
| UX-15 | El panel del prestador devuelve todas las citas mezcladas (`PENDIENTE_PAGO` sin cobro y `CANCELADA`) y expone `client_phone` sin necesidad operativa | PROV §M-05 (`bookingController.js:246-260`) | UX | A | Filtro de estado por defecto y `client_phone` fuera del payload del prestador, con test | abierta |
| UX-16 | `GET /api/providers` se comía los errores y devolvía `200 {count:0,data:[]}`: mapa vacío indistinguible de "no hay prestadores cerca" | PROV §B-02 · cierre en FA-R2 (C1) | HONESTIDAD-DE-ESTADO | H | El error se propaga como 503 con cabecera de degradación | cerrada |
| UX-17 | Un prestador no puede gestionar sus servicios: el catálogo no es editable desde la app y los 133 servicios existentes vienen de semilla | PROV §C-05 | UX | A | El prestador crea/edita un servicio desde la app con test end-to-end | abierta |
| UX-18 | El panel raíz del admin consume un endpoint no montado en el entry real (`grep -c glow-admin index.js` → 0) y el 404 se disfraza de "Modo Simulación" conservando cifras mock | A360-CONS §D-01 y §C-14 (`src/app/page.tsx:44-112,161,174-179`) | HONESTIDAD-DE-ESTADO | A | El endpoint existe o el panel muestra estado vacío/error sin cifras; test de fallo de fetch no pinta GMV | abierta |
| UX-19 | Estado inválido: `pilaCheck` bloquea agendamiento por `suspension_pila`/`pila_estado_verificacion` pero ningún job/endpoint escribe esos campos (nunca suspende solo); su mensaje cita cláusulas del contrato | PROV §M-07 (`pilaCheck.js:23`) | TÉCNICA | A | El estado se alimenta por job/endpoint con test, o el guard se retira | abierta |
| UX-20 | `/register` y `/nueva-cita` del panel son simulaciones (`setTimeout` + `alert`) que no invocan la API | A360-CONS §D-09 (`register/page.tsx:16-23`; `cliente/nueva-cita/page.tsx:17-33`) | UX | A | Ambas pantallas llaman a la API real con test de integración | abierta |
| TEC-01 | Migración de rollback ejecutada antes del `up`: `035_…down.sql` hacía `DROP COLUMN embedding` (y 6 columnas de metadatos) en cada arranque del backend | A360-CONS §C-01 · cierre en REM §2 (C-01: `migrations/rollback/` + filtro `.down.sql` en `index.js`) | TÉCNICA | H | Arrancar el backend dos veces seguidas no cambia `SELECT count(*) FROM beauty_knowledge_embeddings` y el runner no menciona ningún `.down.sql` | cerrada |
| TEC-02 | `npm run migrate` ejecuta `sequelize.sync({force:true})` (DROP TABLE) sin guarda de entorno | A360-CONS §A-10 (`backend/package.json`) | TÉCNICA | A | El script aborta si `NODE_ENV === 'production'`; test que lo comprueba | abierta |
| TEC-03 | Cuatro sistemas de migración conviviendo sin tabla de tracking (`index.js` inline, `migrationRunner.js` muerto, `knexfile.js`, `src/db/migrations/` huérfano) + 3 backups `.bak`/`.backup` + prefijos duplicados `056_*` | A360-CONS §H-10 · REM §3.7 (`migrationRunner.js` no lo requiere nadie) | TÉCNICA | A | Un solo runner con `schema_migrations`; backups fuera del directorio; un error de migración **falla el arranque** en vez de advertir | abierta |
| TEC-04 | Errores de migración silenciados: se registran como advertencia y el runner sigue | A360-CONS §H-05 (`migrationRunner.js:43-46`; `index.js:1638-1645`) | TÉCNICA | A | Una migración no aplicable detiene el arranque con exit ≠ 0 (test) | abierta |
| TEC-05 | Migración `034_add_fks_to_academy_tables.sql` falla siempre (`no unique constraint matching given keys`) porque `033` busca la PK por nombre y PG la llamó `academy_certificates_pkey`: la FK de `qr_certificates` nunca existe y el runner lo silencia | ESC §B2 (`033:14-17`; `008:71`; `034:35`) | TÉCNICA | A | La PK se detecta por columnas (`pg_constraint`/`pg_attribute`), existe `fk_qr_certificates_certificate_id` y el replay 008→034 termina sin error | abierta |
| TEC-06 | Colisión de UUID de seed: `026` reusa los IDs de `008`/`025` con otro `course_id` y su `ON CONFLICT (id) DO UPDATE` solo actualiza `title`/`sort_order`/`content_text`, dejando el curso de colorimetría partido (6 módulos desordenados, 11 de 15 lecciones) y el de bioseguridad con títulos ajenos, en cada arranque | ESC §B1 (replay PG 008→034) | TÉCNICA | A | Test anti-regresión: aplicar 008+026 y comprobar que `c001` no cambia; `026` con namespace nuevo y `DO NOTHING` | abierta |
| TEC-07 | El contenido de las 15 lecciones del curso de colorimetría es un placeholder en la base (`CONTENIDO_LECCION_*_AQUI`) y todas las `video_url` apuntan a `youtube.com/watch?v=dQw4w9WgXcQ` | ESC §B7 (`026:60-82`; `course_detail_screen.dart:266`) | TÉCNICA | A | Script `publish-lesson` carga `docs/academy/**/*.md` en `academy_lessons`; ninguna fila contiene placeholder ni URL de Rick Astley | abierta |
| TEC-08 | El reproductor de lecciones es falso: imagen fija de Unsplash, botón play que solo lanza un SnackBar y rótulo "Streaming HD de lección disponible", ignorando `video_url` | ESC §B12 (`course_detail_screen.dart:195-231`) | TÉCNICA | A | El reproductor usa `video_url` real o el rótulo desaparece (test de widget) | abierta |
| TEC-09 | Tres criterios de aprobación distintos: UI Flutter "100% de las preguntas", admin "requiere 100% aciertos" y backend 80%, con `QUIZ_PASS_PCT` sin definir en ningún `.env`/workflow | ESC §B10 (`quiz_screen.dart:206`; `[id]/page.tsx:694`; `academyRoutes.js:250`) | TÉCNICA | A | Un único umbral leído del backend y expuesto a las dos UIs; test de score límite | abierta |
| TEC-10 | El examen es trivialmente vulnerable: 2 preguntas fijas por curso, intentos ilimitados, sin tiempo ni pool de preguntas | ESC §T3 "Prácticas a mejorar" #2 (`academyRoutes.js:204-282`) | TÉCNICA | A | Banco ≥20 preguntas, N aleatorias, máx. 3 intentos con registro; test de intentos agotados | abierta |
| TEC-11 | No existe matrícula (enrollment): "inscritos" se deriva de `academy_progress` | ESC §T3 "Prácticas a mejorar" #3 (`academyAdminRoutes.js:43-49`) | TÉCNICA | A | Tabla de matrícula con estado/origen/fechas y endpoint que la usa | abierta |
| TEC-12 | "Completado" auto-declarado: el alumno solo pulsa "Marcar como Completada", sin registrar consumo | ESC §T3 "Prácticas a mejorar" #4 (`course_detail_screen.dart:282-290`) | TÉCNICA | A | Se registra tiempo/porcentaje de consumo o el estado se declara no auditable | abierta |
| TEC-13 | El certificado no es verificable: solo fila + `badge_name`, sin URL/QR/firma; `qr_certificates` existe sin usar; y `DELETE /courses/:id` borra en cascada progreso y certificados | ESC §T3 #5/#6 (`academyRoutes.js:255-269`; `023:97`; `008:29-44`) | TÉCNICA | A | `certificate_id` + `verification_url` + endpoint público sin login con estado `valid/revoked`; el curso pasa a soft-delete y los certificados nunca se borran | abierta |
| TEC-14 | Consentimiento Ley 1581 mal instrumentado: revocar deja el registro como recién aceptado (`fecha_revocacion` nunca se escribe), `/consent/status` responde `hasConsent: true` con `aceptado = false`, la UI promete borrado a 12 meses sin job de purga y no se guarda versión del texto ni evidencia de aceptación | ESC §B8 (`academyRoutes.js:118-124,137-148`; `glow_consent_widget.dart:96-100`) | TÉCNICA | A | Revocar escribe `fecha_revocacion` y conserva `fecha_aceptacion`; `hasConsent` solo con `aceptado = true`; job de purga activo o se retira la promesa de la UI | abierta |
| TEC-15 | Gamificación y admin de academia: `moveModule`/`moveLesson` solo mutan estado React (los endpoints `/reorder` existen y no los llama nadie), la tarjeta "Certificados" está hardcodeada a 0 y el editor de 1425 líneas lleva `// @ts-nocheck` | ESC §B9 y §B14 (`[id]/page.tsx:230-237,321-342,777,1`; `academyAdminRoutes.js:248,348`) | TÉCNICA | A | El reorden persiste por los endpoints (test) o se eliminan; "Certificados" cuenta real; `@ts-nocheck` retirado | abierta |
| TEC-16 | `pool.connect()` fuera del `try` en 3 rutas del admin de academia: con Express 4.18 y Node 24 un fallo de pool mata el proceso en vez de devolver 500; y `BEGIN` seguido de `return res.status(400)` deja la conexión en `idle in transaction` al volver al pool | ESC §B5 y §B6 (`academyAdminRoutes.js:61,249,349`; `:63,67-69`) | TÉCNICA | A | `pool.connect()` dentro del `try` con ROLLBACK en todo camino de salida y `try/catch` en los handlers async | abierta |
| TEC-17 | `academy_progress.completed BOOLEAN NOT NULL DEFAULT TRUE`: un INSERT que omita la columna marca la lección como completada | ESC §B16 (`008:32`) | TÉCNICA | A | El default es `FALSE` con migración y test de INSERT parcial | abierta |
| TEC-18 | La capa "Academy Pro" no tiene tablas en producción: `learning_paths`, `path_courses`, `xp_logs`, `user_levels`, `badges`, `qr_certificates`, `events`, `user_consents`, `user_badges` no están en el dump ⇒ `/api/glow-pro/*` da `relation does not exist` | ESC §B17 (`glowProRoutes.js:35,44,55,64`) | TÉCNICA | A | Las tablas existen en producción o las rutas se retiran; test contra el esquema real | abierta |
| TEC-19 | Categorías de curso sin whitelist ni `CHECK` en la base: el admin ofrece 9, la API acepta cualquier string | ESC §B18 (`008:7`) | TÉCNICA | A | `CHECK` o validación con test que rechaza una categoría fuera de la lista | abierta |
| TEC-20 | Cero tests de la academia: ninguna suite de `backend/tests` ni `frontend/test` la menciona | ESC §B19 | TÉCNICA | A | Suite de integración curso→lección→progreso→quiz→certificado en verde | abierta |
| TEC-21 | `submit-quiz` no hace *gating* server-side (no consulta `academy_progress`) y `courses/:id` entrega `content_text` y `video_url` de lecciones bloqueadas | ESC §T3 "Prácticas a mejorar" #1 (`academyRoutes.js:222-282`, `:54-62`) | TÉCNICA | A | Rechazo con 409 sin curso completo y el contenido de lecciones no desbloqueadas no viaja en la respuesta | abierta |
| TEC-22 | El CI está muerto por marcadores de conflicto commiteados en `main` (`.github/workflows/ci.yml:60,84,90` y `.gitignore`): un workflow inválido no lo carga el runner | PROV §B-01 · cierre en FA-R1 (YAML válido) y FA-R2 (`grep -c '^<<<<<<<'` = 0 en `345dfeb9`) | TÉCNICA | H | `python -c "import yaml;yaml.safe_load(open('.github/workflows/ci.yml'))"` sin error y 0 marcadores | cerrada |
| TEC-23 | El CI nunca ha corrido: no hay PR ni run visible, así que ninguna afirmación de "CI en verde" es verificable en este repo | PROV §B-01 · FA-R2 §S3 ("el run nunca se ejecutó") | TÉCNICA | D | Un run de GitHub Actions visible sobre el PR, con los nombres de las suites heredadas declarados | abierta |
| TEC-24 | Deuda declarada: 15 suites rojas heredadas (79 totales) con conteo de tests fallidos inestable (70 en su BASE declarada, 71 y 72 en dos mediciones del auditor) sin cambios en código de tests | FA-R1 §H-05 · FA-R2 §B6 | TÉCNICA | H | Los nombres de las 15 suites en BASE y rama, y `Tests: 0 failed` tras declarar las heredadas | heredada |
| TEC-25 | Suites excluidas del gate de CI: 14 suites fuera por patrón de nombre (~23% de 61 archivos), incluidas todas las de biometría, contratos y resiliencia; 8 de las 15 rojas no están en el `testPathIgnorePatterns` bloqueante | A360-CONS §A-15 (`ci.yml:36`) · FA-R1 §H-05 (`ci.yml:98`) | TÉCNICA | A | `testPathIgnorePatterns` vacío y cada suite arreglada o `describe.skip` explícito y trazable | heredada |
| TEC-26 | El job de lint del workflow RAG ejecuta `npm run lint` cuando ese script no existe: éxito sin medir | A360-CONS §A-12 (`rag-evaluation.yml:60`; `backend/package.json`) | TÉCNICA | A | El lint falla si no existe el script; se añade script real | abierta |
| TEC-27 | El CI escribe en la base de datos remota (`knex migrate:latest` + ingesta con `secrets.RAILWAY_DATABASE_URL`, sin bloque `services:`) | A360-CONS §A-12 (`rag-evaluation.yml:27,125,131`) | TÉCNICA | A | BD efímera `pgvector` en el runner; prohibido `RAILWAY_DATABASE_URL` en workflows de test | abierta |
| TEC-28 | El "Deploy Gate" no despliega: la condición usa `secrets` en `steps[*].if` y apunta a un webhook de Railway; no hay workflow real a EKS/ECR, sin health check ni rollback | A360-CONS §A-13 (`rag-evaluation.yml:232-242`) | TÉCNICA | A | Workflow de deploy con `kubectl rollout status` sobre `/healthz` y `kubectl rollout undo`; `grep -niE 'eks\|ecr' .github/workflows/*.yml` ≠ 0 | abierta |
| TEC-29 | El gate de calidad RAG se invoca con el intérprete equivocado (`node scripts/ciRagEvaluation.sh`; `package.json` declara `bash`): nunca bloquea | A360-CONS §A-14 (`rag-evaluation.yml:155`; `package.json:14`) | TÉCNICA | A | El step usa `bash` y falla cuando la evaluación está por debajo del umbral (mutación comprobada) | abierta |
| TEC-30 | `npm run lint` del admin devuelve exit 2 (config ESLint 9 + `eslint-config-next` 15.x) con `eslint.ignoreDuringBuilds: true`: 0 reglas aplicadas y build siempre verde; quedan 98 errores pendientes (47 `no-unused-vars`, 47 `no-explicit-any`) | A360-CONS §D-05 · REM §3.5 (`eslint.config.mjs:6`) | TÉCNICA | A | `npm run lint` exit 0 y `ignoreDuringBuilds` retirado del `next.config.ts` | parcial (ronda 2) |
| TEC-31 | `@ts-nocheck` en la pantalla admin más grande (CRUD de Academia, 1425 líneas) y 33 `any` | A360-CONS §D-04 · ESC §B14 | TÉCNICA | A | `@ts-nocheck` retirado y `tsc --noEmit` en verde | abierta |
| TEC-32 | Dos tokens en `localStorage` (`glow_token` y `adminToken`) como fuentes de verdad paralelas y 3 variables de entorno distintas para la URL del API | A360-CONS §D-10 (`AuthContext.tsx:58-62`; `business/page.tsx:55`; `app/page.tsx:151`) | TÉCNICA | A | Una sola fuente de token y una sola variable de URL; test que falla con dos fuentes | abierta |
| TEC-33 | El guard de `/admin/business` usa la bandera de datos en vez de la de auth y envía `Bearer 'admin-token'` literal cuando no hay sesión | A360-CONS §D-06 (`admin/business/page.tsx:48-62,70,106,138`) | SEGURIDAD | A | Sin sesión la ruta redirige al login; test de acceso anónimo | abierta |
| TEC-34 | IDOR y bypass por nombre en el repositorio de negocio: `providerId.startsWith('demo')` autoriza cualquier tarea y `UPDATE business_tasks … WHERE id = $1` no valida dueño | A360-CONS §A-03 (`businessRepository.js:292,302-307`) | SEGURIDAD | A | Test de IDOR: un segundo proveedor no puede avanzar la tarea del primero (caso `BUS-SEC-002`, hoy en rojo) | abierta |
| TEC-35 | `req.user.rol` no existe pero se usa: guard de admin que nunca pasa y rol de BD mal reportado | A360-CONS §A-01 · cierre en REM §2 (A-01) | TÉCNICA | H | `auth.js` setea `role` y `rol`; test que falla si un guard lee `req.user.rol` inexistente | cerrada |
| TEC-36 | El login del admin estaba invertido por email (el rol devuelto por `login()` se ignoraba), sin gate de rol (`ProtectedRoute` sin `allowedRoles` y `middleware.ts` no-op) y con `NEXT_PUBLIC_ADMIN_TOKEN` inlineado en el bundle público | A360-CONS §C-14 · cierre en REM §2 (C-14) | SEGURIDAD | H | Redirección por rol del token y `allowedRoles={['ADMIN']}`; sin `NEXT_PUBLIC_*` de admin en el bundle | cerrada |
| TEC-37 | Tres implementaciones distintas de `adminMiddleware` con políticas distintas (uno consulta la BD en cada request, otro confía en el token, un tercero lo replica) | A360-CONS §A-18 (`index.js:44-62`; `middleware/auth.js:63-73`; `middleware/admin.js:4-14`) | SEGURIDAD | A | Una sola implementación exportada y todas las rutas importan de ahí | abierta |
| TEC-38 | `logAccess` se usa pero no se importa en el camino de consentimiento biométrico, y el `ReferenceError` se traga en el `catch` "fallback silencioso" | A360-CONS §A-19 (`geminiService.js:805` vs `:12`) | TÉCNICA | A | El import existe y un test del camino de consentimiento no produce `ReferenceError` | abierta |
| TEC-39 | Interpolación de `tenantId` en SQL del retrieval RAG (único punto sin parametrizar) | A360-CONS §A-20 (`ragService.js:103,152`) | SEGURIDAD | A | Query parametrizada; test con `tenantId` malicioso no rompe el SQL | abierta |
| TEC-40 | Sanitizador inefectivo y destructivo: asigna `req.query`/`req.params` (no-op en Express 4) y su blacklist borra contenido legítimo | A360-CONS "Medios y bajos" (`sanitizer.js:29-40`; `index.js:215-216`) | TÉCNICA | A | El sanitizador usa `Object.defineProperty` o middleware equivalente, con test de contenido legítimo preservado | abierta |
| TEC-41 | Pagos sin idempotencia (el middleware solo se monta en endpoints biométricos) y webhook sin reconciliar `amount` contra `valor_bruto` | A360-CONS "Medios y bajos" (`biometricRoutes.js:19`; `v1/beautyRoutes.js:163`; `bookingController.js:575-650`) | DINERO | A | Test que repite el mismo pago y no duplica; `amount` distinto de `valor_bruto` se rechaza | abierta |
| TEC-42 | `/api/salon` montado dos veces (el segundo montaje es inalcanzable) | A360 §M-04 (`index.js:388` y `:529`) | TÉCNICA | A | Un solo montaje; test de ruta | abierta |
| TEC-43 | Apagado ordenado roto: `const server` en scope de bloque (1728) referenciado en 1781 → `ReferenceError` en SIGTERM y nunca se cierran pool ni Redis | A360 §M-05 (`index.js:1727-1756,1778-1799`) | TÉCNICA | A | `SIGTERM` cierra servidor, pool y Redis con exit 0 (test de señal) | abierta |
| TEC-44 | 11 endpoints inalcanzables en 5 routers huérfanos (`inventoryRoutes`, `learningPathRoutes`, `userLevelRoutes`, `b2bCoPilotRoutes`, `shortcutRoutes`) | A360 §M-06 | TÉCNICA | A | Los routers se montan con cobertura o se borran; `grep` de montaje ≠ 0 | abierta |
| TEC-45 | `src/startup/app.js` es un entry falso de 364 líneas (29 mounts vs 43, 3 rutas que el real no tiene) y su línea 28 requiere `routes/userRoutes`, que no existe | A360 §M-07 · REM §3.9 | TÉCNICA | A | El archivo se borra; `grep -rn "startup/app" backend --include=*.js` = 0 | abierta |
| TEC-46 | `backend/index.js` es un monolito de 1807 líneas con 24 endpoints inline, jobs de pago, SSE, WebSocket, Swagger y el runner de migraciones | A360 §M-08 | TÉCNICA | A | El entry queda como composición de routers con test de inventario de rutas | abierta |
| TEC-47 | Rate limit global laxo: 1000 req/15 min por IP para todo `/api`; solo 2 de 39 archivos de rutas montan limitador propio, así que `login`/OTP/registro quedan bajo ese techo | A360 §M-09 · V23 #14 (`index.js:266-272,347`) | SEGURIDAD | A | Límite estricto en auth/OTP con test de fuerza bruta que se bloquea | abierta |
| TEC-48 | Logs sin estructura ni masking: el logger define `format.json()` pero el transport lo sobrescribe con `colorize().simple()`, sin request-id, y hay 283 `console.log` (magnitud medida, no los 689 que afirmaba otro informe) | A360 §M-10 · V23 #8 (`logger.js:5-20`; `index.js:627`) | TÉCNICA | A | Log estructurado en JSON con request-id y sin `console.log` en `src/` (test de lint) | abierta |
| TEC-49 | `NODE_ENV` como único interruptor de seguridad con criterios contradictorios: `!process.env.NODE_ENV` = producción en biometría y ≠ producción en OTP | A360 §M-11 (`biometricCryptoService.js:10` vs `paymentRoutes.js:164`) | TÉCNICA | A | Un helper único de entorno con test de los dos caminos | abierta |
| TEC-50 | Firma de webhook comparada contra `JSON.stringify(req.body)` en vez del cuerpo crudo (el orden de claves no está garantizado) | A360 §M-14 (`bookingController.js:19-33`; `index.js:202`) | TÉCNICA | A | `express.json({verify})` captura `rawBody` y la firma se calcula sobre el crudo (test con orden de claves alterado) | abierta |
| TEC-51 | `express.json({limit:'50mb'})` y `urlencoded` de 50 MB en todas las rutas: superficie de DoS por memoria | A360 §M-15 (`index.js:202-203`) | SEGURIDAD | A | Límites por ruta con test de cuerpo sobredimensionado rechazado | abierta |
| TEC-52 | `queue.service.js` es código muerto con cola en memoria que silencia errores de Redis: una tarea encolada se pierde al reiniciar el pod | A360 §A-16 (`queue.service.js:22-27`) | TÉCNICA | A | Se borra o se sustituye por el worker real con test de persistencia | abierta |
| TEC-53 | **Fallback del `JWT_SECRET` incondicional** (re-medido 2026-09-24 sobre `jwt.js:1-21`: **no hay ninguna comprobación de entorno**; esta fila afirmaba «solo con `NODE_ENV==='test'`» y eso ya no era cierto — clase R-05): sin la variable, o con menos de 32 caracteres, **todos los tokens se firman con un literal publicado en este repositorio público** ⇒ cualquiera fabrica un token válido y `toApiRole` lo mapea a `admin`. La entrega de A-06 lo rotuló «falso positivo dev» y añadió `glowapp_` a la allowlist del escáner: quedó sin arreglar **y** invisible | medición directa de `jwt.js:5-11` en `fix/compuerta-secretos-reproducible@5020e4df`; mutación `'glowapp_jwt_production_…'` ⇒ escáner `exit 0` (`docs/audit/AUDITORIA-ENTREGA-A-06-2026-09-24.md` §3) | SEGURIDAD | **CRÍTICO** | `getJwtSecret()` lanza fuera de `NODE_ENV=test`; `grep -c DEFAULT_PROD_SECRET backend/src/config/jwt.js` = 0; la mutación `glowapp_…` **falla** la compuerta (ronda 2, C1-C4) | abierta |
| TEC-54 | Redis corta la reconexión tras 3 intentos y el log de error se emite una sola vez | V23 #10 (`redis.js`) | TÉCNICA | A | Reintento con backoff indefinido y aviso repetido (test de caída prolongada) | abierta |
| TEC-55 | `db.js` y `database.js` duplican la configuración de conexión (dos fuentes de verdad para SSL y pool) y `database.js` está vivo (31 requirers) | V23 "Lo que el informe NO vio" #4 (`database.js:30`) | TÉCNICA | A | Una sola fuente de configuración de conexión; test que comprueba que ambas rutas usan el mismo objeto | abierta |
| TEC-56 | URLs de producción hardcodeadas: 46 ocurrencias de `railway.app`, 3 en rutas vivas (`authController.js:468` link de referidos, `salonController.js:180` invitación, `emailService.js:7` checkout) | V23 #22 | TÉCNICA | A | Las 3 rutas leen `FRONTEND_URL`; test que falla con literal `railway.app` en `src/` | abierta |
| TEC-57 | 8 `.dart` muertos en `backend/` y 5 `.backup`/`.bak`, más `contextController.dart` huérfano | V23 #6/#7 | TÉCNICA | A | Los archivos se borran; `git ls-files` no los lista | abierta |
| TEC-58 | `backend/scripts/smokeSurfaces.js` (guardián de Fase A) depende de `/api/health` —el endpoint que vigila— para saber si el servidor está degradado | FA-R2 §B3 (`smokeSurfaces.js:56`) | TÉCNICA | A | El guardián determina la degradación por una fuente independiente del endpoint vigilado | abierta |
| TEC-59 | El delta de 1–2 tests entre BASE y rama de Fase A queda sin atribuir: no hay lista de nombres en ninguno de los dos lados | FA-R1 §H-08 · FA-R2 §B6 | TÉCNICA | H | Nombres de suite en BASE y rama con cierre explícito del delta | abierta |
| TEC-60 | 6 ramas se dibujan naciendo del fondo del tronco (arista inventada): cuando el `merge-base` no está en los últimos 40 commits, el script usa `mbY = trunkEndY` | GRAFO §D1 (líneas 260-263; seis arcos `M 520 1906` contra merge-bases reales `5334c31b`, `b67b4c48`, `a5cc3214`) | TÉCNICA | A | Los 6 arcos nacen en su SHA real o llevan etiqueta "fork anterior al tramo visible"; cero `M 520 1906` para esas ramas | abierta |
| TEC-61 | La leyenda del PNG lleva los conteos hardcodeados (`ACTIVAS (5), HUÉRFANAS (4), MUERTAS (15), BASE (2)`) y la caja del fósil son literales (`1363`, `22036`) con `catch` vacío | GRAFO §D2 y §D3 (líneas 370-373; 66; 352,355) | TÉCNICA | A | Cambiar un conteo a mano en el código no cambia la imagen; si el fósil no responde la caja dice "no verificado" | abierta |
| TEC-62 | `refs/remotes/origin/HEAD` se abrevia a `origin` y aparece como una rama BASE inexistente, contaminando los conteos | GRAFO §D4 (refs/heads = 23, refs/remotes/origin = 21) | TÉCNICA | A | `BASE=1` (solo `main`) y los nombres únicos bajan en 1 | abierta |
| TEC-63 | El PNG no se puede regenerar (`ModuleNotFoundError: svglib`), el fallo se traga y el mensaje promete 2x cuando el factor real es 1.5x | GRAFO §D5 (líneas 428-437) | TÉCNICA | A | Con `svglib` ausente la corrida aborta con ≠ 0 y el mensaje dice el factor real | abierta |
| TEC-64 | El artefacto commiteado no se reproduce: el SVG tiene otro SHA-256 que una corrida nueva y documenta el estado anterior a su propio commit (`cfec993a · +5` vs `c1069e9f · +6`) | GRAFO §D6 | TÉCNICA | A | `sha256sum` del SVG commiteado == el de una corrida nueva; script y artefactos en commits separados o rótulo `estado medido en <sha>` | abierta |
| TEC-65 | La fecha de referencia del grafo está fija (`const refDate = new Date('2026-09-24')`): la regla "≤ 14 días" queda congelada y las clases cambian solas con el tiempo | GRAFO §D7 (línea 113) | TÉCNICA | A | `refDate` = fecha real de la corrida; corrido mañana el título cambia y las clases no | abierta |
| TEC-66 | Legibilidad del grafo: los mensajes del tronco se dibujan a `x=340` y se salen por la izquierda del lienzo, los arcos verdes cruzan esa franja y el conector del fósil cruza etiquetas | GRAFO §D8 (líneas 239; texto x=10-340 vs tarjetas x=80-390) | TÉCNICA | A | Ningún `<text>` con `x_texto − ancho_estimado < 0` y sin solape con las tarjetas | abierta |
| TEC-67 | 672 `.md` sueltos en la raíz (221 `F7.*`, 303 `GIA-*`, 65 `D00*`, 12 `D-00*`, 71 otros) + 5 scripts sueltos + `test_face.jpg` + `0)ls` + 4 `.report`: 691 archivos y 2,53 MB | A360-CONS §H-06 | TÉCNICA | A | Los informes pasan a `docs/governance/` con índice; raíz sin `.md` sueltos | abierta |
| TEC-68 | `backend/public/` está trackeado con 60 archivos y 52,7 MB, incluido `main.dart.js` de 189.468 líneas (build de Flutter Web versionado como fuente) | A360-CONS §H-07 | TÉCNICA | A | `backend/public/` fuera del índice y en `.gitignore`; el build se genera en CI | abierta |
| TEC-69 | `.hermes/` está trackeado (14 archivos) con un volcado de sesión de 588 KB y un `.pyc` (sin credenciales reales: la contraseña estaba redactada como `***`) | A360-CONS §H-08 y §5 (refutación del "crítico") | TÉCNICA | A | `.hermes/` fuera del índice; el volcado se purga | abierta |
| TEC-70 | 8 vulnerabilidades `high` en dependencias de producción del backend, y `admin-dashboard` se medía sin `package-lock.json` (npm audit imposible: ENOLOCK) | A360-CONS §H-09 | TÉCNICA | H | `npm audit` sin `high` en backend y lockfile presente en `admin-dashboard` con auditoría ejecutable | abierta |
| TEC-71 | El wrapper de Gradle estaba versionado con 22.043 archivos y 532 MB (`git ls-files frontend/android/gradle/wrapper`) | A360-CONS §H-01 · cierre en REM §2 (H-01: fuera del índice + `.gitignore`) | TÉCNICA | H | `git ls-files frontend/android/gradle/wrapper \| wc -l` = 0 | cerrada |
| TEC-72 | `migrationRunner.js` es código muerto (nadie lo requiere): el filtro efectivo del bug de rollbacks es otro | REM §3.7 | TÉCNICA | A | El archivo se borra o se convierte en el runner único | abierta |
| TEC-73 | Flutter residual: 49 warnings de código muerto (`unused_import/field/element`) preexistentes y 2 errores en `inventory_alert_dialog.dart` que llama a `ApiService.fetchConsignmentInventory`/`consumeInventoryItem` (no existen) | REM §3.6 · A360-CONS §F-10 (`flutter analyze lib` → 2 error) | TÉCNICA | A | `flutter analyze` con 0 errores y sin `unused_*` en el baseline | parcial (ronda 2) |
| TEC-74 | El baseline de Flutter es inestable entre mediciones (2 errores / 535 info en A360; 2 errores / 52 warnings / 533 info en REM; 0 errores / 38 warnings / 486 infos en FA-R2) y 87 usos de `withOpacity` deprecado siguen vivos | A360-CONS "Frontend medios" · REM §1 · FA-R2 "Verificado a su favor" | TÉCNICA | H | Un baseline único y versionado de `flutter analyze` con el comando y la rama declarados | abierta |
| TEC-75 | 56 archivos `.dart` sin importador alcanzable y 12 rutas declaradas y nunca navegadas por nombre | A360-CONS "Frontend medios/bajos" | TÉCNICA | A | Cero archivos sin importador y cero rutas huérfanas (script de verificación) | abierta |
| TEC-76 | El JWT del frontend se guarda además en `SharedPreferences` en claro y el rol del token se decodifica y nunca se lee (`_userRole`, `_hasToken` sin uso): sin gateo por rol, sin validar `exp`, sin manejo de 401 | A360-CONS §F-07 y §F-08 (`main.dart:328-329,838-858`; `auth_service.dart:43-44,353-354`) | SEGURIDAD | A | Almacenamiento seguro del token, validación de `exp` y manejo de 401 con test de rol | abierta |
| TEC-77 | `stagingUrl` apunta a producción, hay JWT y payload en logs de debug, `catch` vacíos que tragan errores, 9 `print` en producción y tres `late final` sin inicializar | A360-CONS "Frontend medios/bajos" (`main.dart:341-343`) | TÉCNICA | A | `stagingUrl` correcto, sin `print` ni token en logs; `flutter analyze` sin `unused` en esas líneas | abierta |
| TEC-78 | El informe de 23 hallazgos del backend contenía 7 falsos positivos, 2 desactualizados y una cifra inflada (689 `console.log` vs 283 medidos) | V23 "Cuadro de veredictos" (#5, #8, #15, #17, #18, #20, #21, #23) | TÉCNICA | H | Cada hallazgo nuevo cita `archivo:línea` + comando ejecutado; los refutados no se reabren | cerrada |
| TEC-79 | Restan herramientas de triage/alineación de ramas y su política en CI (P1 y P2 siguen vigentes tras la poda) | PODA §6.3 · POL §8 (`scripts/checkAlignment.sh` y job semanal de CI no existen) | TÉCNICA | A | `scripts/checkAlignment.sh` y el job semanal fallan en los 5 supuestos de POL §8 | abierta |
| TEC-80 | `delete_branch_on_merge = on` no está activado en el repositorio: un merge sin borrado deja ramas que parecen vivas | PODA §6.2 · POL §2.4 | TÉCNICA | D | El ajuste está activo y una rama mergeada desaparece tras el merge | abierta |
| TEC-81 | Decisión pendiente sobre el clon fósil: 331 commits únicos en 14 ramas, cinco con nombres sensibles (`feature/implement-encryption-at-rest`, `feature/integrate-secret-manager`…) y el repo es público ⇒ no se publicaron; quedan en un bundle local de 319 MB | PODA §3.2 y §6.4 | TÉCNICA | D | Decisión registrada: se publica (con revisión de las 5 ramas) o se archiva fuera del repo | abierta |
| TEC-82 | La poda de ramas se ejecutó con un gate que no era el anunciado: la consulta de PRs por API falló en esa corrida y la protección vino de una lista fija del script | PODA §4.1 | TÉCNICA | H | La lista de PRs se lee en la misma corrida que la poda (POL §5/§7 lo exige) | cerrada |

---

## Hallazgos cerrados con evidencia (commit o comando que lo probó)

| ID | Hallazgo | Prueba del cierre |
|---|---|---|
| DIN-13 | Simulador de pago en 4 sitios | `INFORME-REMEDIACION §2 (C-05)`: `payBooking`, `crearPayout` y `payMedicalValidation` → 501/rechazo sin `ALLOW_PAYMENT_SIMULATOR`; `npx jest src/tests/audit360-remediation.test.js` → **21/21 verde** (commits `dc8c570c`, `451e538b`) |
| SEG-01 | WebSocket sin autenticación | `INFORME-REMEDIACION §2 (C-04)`: token obligatorio, pertenencia verificada, `providerId` desde el token; clientes `chat_screen.dart:176` y `booking_tracking_screen.dart:67` envían token (commit `dc8c570c`/`451e538b`) |
| SEG-05 | Password/claves/OTP en logs | `INFORME-REMEDIACION §2 (C-06)`; criterio de aceptación: `grep -rn "Password:" backend/src` → 0 |
| SEG-06 | Clave biométrica derivada del `JWT_SECRET` | `V23 #4`: `biometricCryptoService.js:33` **lanza error** si se intenta derivar; usa `BIOMETRIC_KEY` (`:53`). `INFORME-REMEDIACION §2 (C-11)`: fail-closed + `decryptWithLegacyKey()` + `scripts/reencryptBiometricData.js` |
| SEG-07 | Fuga cross-usuario del caché semántico | `INFORME-REMEDIACION §2 (C-12)`: identidad en la clave y en el índice de candidatos (`semanticCache.js`) |
| SEG-18 | `/api/ai/orchestrate` abierto con guarda decorativa | `INFORME-REMEDIACION §2 (A-17)`: auth+admin y guard acotado al repo |
| SEG-22 / UX-16 / UX-07 | `/api/providers` devolvía 200 con datos fabricados | `FA-R2 (C1)`: `curl /api/providers` con base inalcanzable → `HTTP/1.1 503` + `X-GlowApp-Degraded: memory-fallback` + `{"error":"PROVIDER_SEARCH_DEGRADED"}` |
| HON-04 | Fail-open a datos fabricados en `db.js` (núcleo) | `INFORME-REMEDIACION §2 (C-03)`: sin cortocircuito a `handleMemoryQuery`, `isPgAvailable` recuperable, `testConnection` no miente, `ALLOW_MEMORY_FALLBACK`, `getDbStatus()`; `curl /api/health` con BD caída → `503 {"servingFabricatedData":true}` |
| HON-06 / API-02 | Contrato IA + métricas clínicas falsas | `INFORME-REMEDIACION §2 (C-07/C-08)`: worker exige 4 archivos multipart, métricas reales por imagen (75.0 vs 89.0), 422/400/413; `python ai_worker/tests/test_beauty_scan_contract.py` → **7/7 OK** |
| HON-08 | Nota clínica aleatoria firmada como dermatólogo | `INFORME-REMEDIACION §2 (C-10)`: eliminada de `designsController` |
| TEC-01 | Rollback `DROP COLUMN embedding` en cada arranque | `INFORME-REMEDIACION §2 (C-01)`: `backend/migrations/rollback/` + filtro `.down.sql` en `index.js` |
| TEC-22 | CI muerto por marcadores de conflicto | `FA-R1`: `python -c "import yaml;yaml.safe_load(...)"` → OK y `grep -c '^<<<<<<<'` → 0; `FA-R2`: `git show 345dfeb9:backend/scripts/checkNoConflictMarkers.js \| grep -c '^<<<<<<<'` → **0** |
| TEC-35 | `req.user.rol` inexistente | `INFORME-REMEDIACION §2 (A-01)`: corregido en `auth.js` |
| TEC-36 | Login admin invertido por email y sin gate de rol | `INFORME-REMEDIACION §2 (C-14)`: rol desde el token + `allowedRoles={['ADMIN']}` |
| TEC-71 | Wrapper de Gradle de 532 MB versionado | `INFORME-REMEDIACION §2 (H-01)`: fuera del índice + `.gitignore` |
| TEC-78 | Informe de 23 hallazgos con falsos positivos | `V23`: 7 refutados ([X]), 2 desactualizados, cifra corregida (283 logs medidos) |
| TEC-82 | Gate de poda no era el anunciado | `PODA §4.1`: declarado como error propio; el gate correcto queda exigido en POL §7 |
| UX-08 | `error.message` expuesto en ruta pública | `FA-R2`: `providerController` devuelve `500 {success:false,error:'INTERNAL_SERVER_ERROR'}` sin `message` |
| UX-06 | Entrega de Fase A sin commits ni PR (ronda 1) | `FA-R2`: 5 commits encadenados y pusheados (`345dfeb9 → cfec993a`), árbol limpio, marcador del índice resuelto |

---

## Hallazgos que requieren decisión del dueño (Diego)

| ID | Qué hay que decidir | Referencia | Consecuencia de no decidir |
|---|---|---|---|
| DIN-01 / DIN-14 | ¿Se integra la pasarela Wompi o se retira el cobro del producto (incluida la tienda GlowShop)? Es la decisión que el propio informe reserva al director | `A360-CONS §C-05` ("Decisión de negocio pendiente"); `PROV §7 Fase 6`; `REM §3.1` | El módulo del prestador no cierra: sin cobro no hay reserva pagada, ni wallet, ni payout |
| SEG-23 (con SEG-13) | ¿Una sola verificación — OTP de 6 dígitos con canal real, o PIN de 4 dígitos validado en servidor? Hoy hay dos incompatibles y el backend ignora una | `PROV §C-04` y `§7 Fase 2` | El prestador cree verificar la entrega y el sistema no lo registra |
| SEG-02 / SEG-03 | Ventana de aplicación de `manual/069_force_rls_strict_isolation.sql` y `070_reassign_tenant_id_by_owner.sql`: activar FORCE RLS sin contexto de tenant en todas las rutas devuelve 0 filas globalmente | `REM §3.2`; `V23 #16` | O la app no filtra nada (RLS inerte) o el panel devuelve 0 filas en silencio |
| SEG-04 | Rotación de los 7+1 secretos y purga del historial de git (`git filter-repo`/BFG con force-push coordinado) | `A360-CONS §C-02`; `REM §3.3` | Los secretos siguen válidos en el historial de un repo público |
| DIN-09 / DIN-10 | Fuente única de la comisión (leer `platform_config.comision_plataforma_pct` **o** el trigger, nunca ambos) y aplicar o retirar `cancelacion_libre_horas`, `disputa_max_reembolso_pct`, `riesgo_suspender_score` | `PROV §7 Fase 4` | La UI sigue comunicando un 20% que no existe y prometiendo reglas que no se ejecutan |
| UX-05 / TEC-80 | Crear el PR de `fase-a/verdad-operativa` y activar `delete_branch_on_merge = on` en Settings (requiere UI/token del dueño) | `FA-R2 §B1`; `PODA §6.1 y §6.2` | Sin PR no hay run de CI verificable y sin el ajuste la poda se deshace sola |
| TEC-81 | Publicar o no el clon fósil (331 commits, 5 ramas con nombres sensibles, repo público) | `PODA §3.2 y §6.4` | O se pierde trabajo o se publican nombres que revelan contextos sensibles |
| TEC-79 | Autorizar las herramientas de triage/alineación (P1, P2) y su job semanal de CI | `PODA §6.3`; `POL §8` | Se repite el problema original: auditorías sobre copias desalineadas |
| HON-09 / HON-12 | Política de producto para los modos demo (qué se puede simular y cómo se declara) y el copy de la tienda | `A360-CONS §C-13`; `FA-R2 §B4` | Se cambia una mentira por otra, en la fase cuyo objetivo era dejar de mentir |

---

## Lo que NO se ha podido verificar

| Tema | Por qué no | Fuente | Qué medición lo resuelve |
|---|---|---|---|
| Entorno real de producción | No se puede leer la consola de Railway desde el entorno de auditoría; el CLI no tiene permisos de lectura de variables | `PROV §6` (`NODE_ENV`, `ALLOW_PAYMENT_SIMULATOR`, `EXPOSE_DEV_OTP`, `WOMPI_WEBHOOK_SECRET`) | Consola de Railway y `SELECT relforcerowsecurity` sobre la BD real |
| Rol de conexión de la app a Postgres | Depende de la credencial de producción, no legible desde el repo | `PROV §M-02` | Decidir si RLS filtra o si el rol lo salta (superusuario/BYPASSRLS) |
| `068_force_rls_strict_isolation.sql` aplicada en Railway | Sin acceso a la BD productiva | `PROV §6`; `V23 #16` | Verificar `relforcerowsecurity` en producción tras el primer boot |
| Estado real de los datos | Ninguna auditoría ejecutó consultas contra la base productiva (regla de solo lectura); lo relativo a embeddings, tenant `demo` y FKs es hipótesis | `A360-CONS §6.1` | `SELECT count(*), count(embedding) FROM beauty_knowledge_embeddings`; `SELECT tenant_id, count(*) FROM usuarios GROUP BY 1` |
| Vigencia de las claves expuestas | No se hicieron llamadas de red con credenciales ni se puede saber si ya fueron rotadas | `A360-CONS §6.3` | Probar/rotar en cada proveedor |
| Pipeline real de despliegue | No hay workflow de deploy versionado; si existe configuración externa (EKS, Helm, aprobaciones) no es auditable desde el repositorio y `gh` no está disponible en Windows | `A360-CONS §6.4`; `FA-R2 §B1` | Consola de GitHub/AWS y el run del PR |
| Cumplimiento del esquema real con lo que el código asume | `bookings.comision_plataforma` NULL/no NULL, existencia de `salones`/`salon_miembros`, tipo de `memberships.user_id` | `A360-CONS §6.5` | `information_schema.columns` sobre el snapshot y sobre producción |
| Vulnerabilidades de dependencias | Los 8 `high` del backend los reportó un subagente y no se re-ejecutó la auditoría; `admin-dashboard` no se podía auditar (ENOLOCK) | `A360-CONS §6.6` | `npm audit` en backend y admin-dashboard con lockfile |
| Contrato real del worker IA en runtime | Se verificaron URLs y modelos, no se ejecutó un request contra `ai_worker` | `A360-CONS §6.2` | `curl -X POST` con el body que envía el proxy y comprobar 422 |
| Comportamiento observado en producción | No se levantó la app ni el backend; ningún hallazgo describe runtime real salvo los conteos de test y análisis estático | `A360-CONS §6.7` | Instrumentación en el entorno desplegado |
| Salud del flujo de cierre del prestador en datos reales | El snapshot muestra 0 OTP, 0 `wallet_transactions`, 0 retiros, 0 disputas y solo 3 citas `COMPLETADA` | `PROV §6` (lectura del snapshot) | Que un prestador real complete el flujo y se cuente |
| Delta de 1–2 tests de la suite backend | Falta la lista de nombres en BASE y rama; el conteo de fallos varía (70/71/72) sin cambios en código de tests | `FA-R1 §H-08`; `FA-R2 §B6` | Correr BASE y rama con `--listTests` y diff de nombres |
| Conexión de la app a la base real con credenciales | No se manejan secretos | `FA-R1 "Lo que NO verifiqué"` | Prueba de humo con credenciales en un entorno controlado |

---

## Ambivalencias de las fuentes (no resueltas aquí)

- `flutter analyze`: 2 errores en [A360-CONS] y [REM] frente a **0 errores** medidos en [FA-R2] — mediciones sobre ramas distintas (`inventory_alert_dialog.dart` vs rama de Fase A). No es contradicción demostrada ni cierre demostrado.
- `admin-dashboard` sin `package-lock.json` en [A360-CONS §H-09] frente a la presencia de `admin-dashboard/package-lock.json` en el árbol actual.
- Conteo de suites rojas: 5–6 en [A360], 13 en el run completo, 15 en [FA-R1]/[FA-R2]; y tests fallidos 37 vs 57 vs 70/71/72.
- Suites excluidas: [A360-CONS §A-15] dice 14 excluidas (~23% de 61 archivos); [FA-R1 §H-05] dice 8 de las 15 rojas fuera del `testPathIgnorePatterns`. Son dos conjuntos distintos, no el mismo dato.
- Estado de la academia: [ESC] documenta daño de datos reproducible, mientras [REM §3.8] registra trabajo de otro agente (`academyService.js`, `academyAdminRoutes.js`, migraciones 066/067) **sin commitear y sin declarar cierre** de B1/B2.
- Total de hallazgos de [A360-CONS]: 169 según su §1, con subtotales que incluyen doble conteo reconocido por el propio informe.
- Mocks biométricos del frontend ([A360-CONS §C-08]): el cierre de [REM §2] cubre el worker y el proxy, no los mocks de `biometric_service.dart`/`processing_screen.dart`.
