# F7.001-F.6.1 — VALIDACIÓN CONTROLADA EN STAGING BLOQUEADA

No es posible proceder con la validación en el entorno actual porque:
- El servicio de PostgreSQL no está disponible (connection refused).
- Sin una base de datos en ejecución, no se pueden ejecutar las migraciones ni probar el mecanismo de retención.
- El entorno de desarrollo local no tiene una instancia de PostgreSQL accesible en el puerto predeterminado.

Para continuar con la validación, se requiere:
1. Una base de datos PostgreSQL en ejecución (puede ser mediante `docker-compose up` en el entorno de staging o desarrollo).
2. Configurar las variables de entorno apropiadas para apuntar a esa instancia.
3. Ejecutar las migraciones para crear las tablas necesarias.
4. Luego proceder con las pruebas de validación descritas en F7.001-F.6.1.

Dado que no se puede iniciar servicios sin autorización explícita y el entorno actual no tiene la base de datos disponible, la validación queda bloqueada.

## Próximos pasos (pendientes de autorización)
Si se autoriza, se podría:
1. Iniciar el servicio de PostgreSQL (por ejemplo, con `docker-compose up -d postgres` si existe un servicio definido).
2. Ejecutar las migraciones recién creadas.
3. Configurar variables de entorno de prueba (NODE_ENV=staging, RETENTION_ENABLED=true, RETENTION_DRY_RUN=true, MAX_ROWS_PER_RUN=1000).
4. Ejecutar la validación completa siguiendo el plan de F7.001-F.6.1.

## Estado actual
**BLOCKED** — Falta de infraestructura de base de datos necesaria para ejecutar la validación.

---  
*Informe generado para F7.001-F.6.1*  
*Fecha: 30 de agosto de 2026*  
*Se detiene aquí conforme a la regla de no avanzar a producción ni ejecutar servicios sin autorización.*