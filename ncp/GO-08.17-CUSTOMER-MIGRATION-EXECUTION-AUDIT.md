# GO-08.17 — CUSTOMER / CLIENT DIRECTORY MIGRATION EXECUTION & PHYSICAL VALIDATION AUDIT
## GLOWAPP SaaS: INFORME DE EJECUCIÓN Y VALIDACIÓN FÍSICA EN POSTGRESQL (MIGRACIÓN 073)

**ESTADO DE EJECUCIÓN:** `EXECUTED SUCCESSFULLY`  
**ESTADO DE VALIDACIÓN FÍSICA:** `PHYSICAL VALIDATION PASS (0 ERRORS)`  
**ESTADO DE REGRESIÓN:** `NO REGRESSION (065-073 PASS)`  
**ESTADO DE IMPLEMENTACIÓN:** `NOT AUTHORIZED`  
**AUTORIDAD DIRECTIVA:** Director del Proyecto GlowApp SaaS (GO-08.17)  
**BASE INMUTABLE:** NODO-01 a NODO-08 (`CLOSED / IMMUTABLE 🔒`)  
**ARCHIVO EJECUTADO:** [`backend/migrations/073_saas_customers.sql`](file:///backend/migrations/073_saas_customers.sql)  
**FECHA:** 2026-09-12  

---

## 1. PRE-FLIGHT AUDIT

Previo a la ejecución de la migración 073, se validaron rigurosamente las condiciones iniciales del entorno:

| Check Pre-Flight | Condición Esperada | Resultado Observado | Estado |
| :--- | :--- | :--- | :---: |
| **Docker / PostgreSQL Container** | Contenedor activo y saludable | `beauty-postgres` Up & Healthy en puerto 5435 | ✅ **PASS** |
| **Database Connection** | Base de datos `beauty_db` accesible | Conexión activa y autenticada | ✅ **PASS** |
| **Migraciones Previas 065–072** | Registradas en `schema_migrations` | 065 a 072 presentes y ordenadas | ✅ **PASS** |
| **Migración 073 en schema_migrations**| NO debe existir previamente | No registrada previo a ejecución | ✅ **PASS** |
| **Tablas Previas Customer** | `saas_customers` y `saas_customer_establishments` inexistentes | 0 tablas encontradas | ✅ **PASS** |

---

## 2. EJECUCIÓN DE LA MIGRACIÓN

La migración fue ejecutada de manera transaccional e idempotente:
- **Comando:** Inyección directa del archivo `backend/migrations/073_saas_customers.sql` al motor PostgreSQL.
- **Resultado del bloque DDL:**
  ```text
  BEGIN
  CREATE TABLE (saas_customers)
  CREATE INDEX (idx_customers_tenant_phone)
  CREATE INDEX (idx_customers_tenant_name)
  CREATE INDEX (idx_customers_user_id)
  ALTER TABLE (ENABLE ROW LEVEL SECURITY)
  DO (CREATE POLICY tenant_isolation_saas_customers)
  CREATE TABLE (saas_customer_establishments)
  CREATE INDEX (idx_cust_est_lookup)
  ALTER TABLE (ENABLE ROW LEVEL SECURITY)
  DO (CREATE POLICY tenant_isolation_saas_customer_establishments)
  INSERT 0 1 (schema_migrations)
  COMMIT
  ```
- **Código de salida:** `0` (Sin errores ni advertencias).

---

## 3. REGISTRO EN `schema_migrations`

Validación física en la tabla de control de migraciones:

```sql
SELECT id, filename, applied_at FROM schema_migrations WHERE filename = '073_saas_customers.sql';
```

| ID | Filename | Applied At |
| :---: | :--- | :--- |
| 19 | `073_saas_customers.sql` | `2026-09-12 18:35:16.462595+00` |

---

## 4. VALIDACIÓN DE TABLAS CREADAS

Se verificó la existencia exclusiva de las dos tablas autorizadas por la arquitectura física:

| Tabla Física | Esquema | Estado en Engine |
| :--- | :---: | :---: |
| `saas_customers` | `public` | ✅ Presente |
| `saas_customer_establishments` | `public` | ✅ Presente |

*(Ninguna otra tabla adicional fue creada por la migración).*

---

## 5. VALIDACIÓN FÍSICA: `saas_customers`

### 5.1 Estructura de Columnas y Tipos de Datos

| Columna | Tipo de Dato | Nullable | Default | Propósito / Decisión |
| :--- | :--- | :---: | :--- | :--- |
| `id` | `UUID` | **NO** | `gen_random_uuid()` | Identificador canónico único |
| `tenant_id` | `INTEGER` | **NO** | *None* | Aislamiento multi-tenant organizacional |
| `user_id` | `INTEGER` | **YES** | *None* | Vínculo opcional/reversible B2C (DEC-CUST-002) |
| `first_name` | `VARCHAR(100)` | **NO** | *None* | Nombre obligatorio |
| `last_name` | `VARCHAR(100)` | **NO** | `''` | Apellidos |
| `phone` | `VARCHAR(30)` | **NO** | *None* | Teléfono de contacto |
| `email` | `VARCHAR(255)` | **YES** | *None* | Correo electrónico opcional |
| `birth_date` | `DATE` | **YES** | *None* | Fecha de nacimiento para fidelización |
| `status` | `VARCHAR(30)` | **NO** | `'ACTIVE'` | Ciclo de vida (`ACTIVE`, `INACTIVE`, `ARCHIVED`) |
| `created_by_membership_id` | `UUID` | **NO** | *None* | Actor de auditoría de creación |
| `created_at` | `TIMESTAMPTZ` | **NO** | `CURRENT_TIMESTAMP` | Marca temporal de creación |
| `updated_at` | `TIMESTAMPTZ` | **NO** | `CURRENT_TIMESTAMP` | Marca temporal de actualización |

### 5.2 Restricciones de Integridad en `saas_customers`

- **PRIMARY KEY:** `saas_customers_pkey PRIMARY KEY (id)`
- **UNIQUE COMPUESTO:** `uq_customer_id_tenant UNIQUE (id, tenant_id)` (Soporte FKs compuestas hijas).
- **CHECK STATUS:** `chk_customer_status CHECK (status IN ('ACTIVE', 'INACTIVE', 'ARCHIVED'))`
- **FOREIGN KEY TENANT:** `fk_customers_tenant FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE`
- **FOREIGN KEY USER (COMPUESTA):** `fk_customers_user FOREIGN KEY (user_id, tenant_id) REFERENCES usuarios(id, tenant_id) ON DELETE SET NULL`
- **FOREIGN KEY CREATOR:** `fk_customers_creator FOREIGN KEY (created_by_membership_id) REFERENCES memberships(id) ON DELETE RESTRICT`

---

## 6. VALIDACIÓN FÍSICA: `saas_customer_establishments`

### 6.1 Estructura de Columnas y Tipos de Datos

| Columna | Tipo de Dato | Nullable | Default | Propósito / Decisión |
| :--- | :--- | :---: | :--- | :--- |
| `id` | `UUID` | **NO** | `gen_random_uuid()` | Identificador único de relación |
| `tenant_id` | `INTEGER` | **NO** | *None* | Aislamiento multi-tenant compuesto |
| `customer_id` | `UUID` | **NO** | *None* | Referencia a `saas_customers(id)` |
| `establishment_id` | `UUID` | **NO** | *None* | Referencia a `establishments(id)` |
| `local_notes` | `TEXT` | **YES** | *None* | Notas privadas locales de sede |
| `is_active` | `BOOLEAN` | **NO** | `true` | Estado activo en la sede |
| `first_visited_at` | `TIMESTAMPTZ` | **NO** | `CURRENT_TIMESTAMP` | Primera visita registrada |
| `last_visited_at` | `TIMESTAMPTZ` | **NO** | `CURRENT_TIMESTAMP` | Última visita registrada |
| `created_at` | `TIMESTAMPTZ` | **NO** | `CURRENT_TIMESTAMP` | Marca temporal de creación |
| `updated_at` | `TIMESTAMPTZ` | **NO** | `CURRENT_TIMESTAMP` | Marca temporal de actualización |

### 6.2 Restricciones de Integridad en `saas_customer_establishments`

- **PRIMARY KEY:** `saas_customer_establishments_pkey PRIMARY KEY (id)`
- **UNIQUE CLIENTE-SEDE:** `uq_customer_per_establishment UNIQUE (establishment_id, customer_id)`
- **FOREIGN KEY TENANT:** `fk_cust_est_tenant FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE`
- **COMPOSITE FOREIGN KEY CUSTOMER:** `fk_cust_est_customer FOREIGN KEY (customer_id, tenant_id) REFERENCES saas_customers(id, tenant_id) ON DELETE CASCADE`
- **COMPOSITE FOREIGN KEY ESTABLISHMENT:** `fk_cust_est_establishment FOREIGN KEY (establishment_id, tenant_id) REFERENCES establishments(id, tenant_id) ON DELETE RESTRICT`

---

## 7. AUDITORÍA DE ÍNDICES FÍSICOS

Se constató que existen única y exclusivamente los índices autorizados en la arquitectura física §10:

| Tabla | Nombre del Índice | Definición Física | Estado |
| :--- | :--- | :--- | :---: |
| `saas_customers` | `idx_customers_tenant_phone` | `USING btree (tenant_id, phone)` | ✅ **PASS** |
| `saas_customers` | `idx_customers_tenant_name` | `USING btree (tenant_id, first_name, last_name)` | ✅ **PASS** |
| `saas_customers` | `idx_customers_user_id` | `USING btree (user_id) WHERE (user_id IS NOT NULL)` | ✅ **PASS** |
| `saas_customer_establishments` | `idx_cust_est_lookup` | `USING btree (establishment_id, customer_id)` | ✅ **PASS** |

*(No existen índices adicionales no autorizados).*

---

## 8. ROW LEVEL SECURITY (RLS)

- **RLS Habilitado:**
  - `saas_customers`: `relrowsecurity = true`
  - `saas_customer_establishments`: `relrowsecurity = true`
- **Políticas Aplicadas:**
  - `tenant_isolation_saas_customers` (`FOR ALL` evaluando `tenant_id = NULLIF(current_setting('app.tenant_id', true), '')::integer`).
  - `tenant_isolation_saas_customer_establishments` (`FOR ALL` evaluando `tenant_id = NULLIF(current_setting('app.tenant_id', true), '')::integer`).

---

## 9. PRUEBAS DE SEGURIDAD NEGATIVA Y AISLAMIENTO

Ejecutadas en entorno transaccional aislado con rollback automático:

1. **Aislamiento RLS en Consulta:**
   - Consulta con rol `beauty_app_user` sin `app.tenant_id`: Retorna **0** filas.
   - Consulta con rol `beauty_app_user` con `app.tenant_id = '999999'` (Tenant B): Retorna **0** filas.
   - Consulta con rol `beauty_app_user` con `app.tenant_id = '1'` (Tenant A): Retorna exactamente **1** fila.
   - **Resultado:** ✅ **PASS**.

2. **Protección contra Cross-Tenant Establishment Linking:**
   - Intento de vincular un `saas_customer` de Tenant 1 con un `establishment` de Tenant 2.
   - **Resultado:** PostgreSQL rechazó la operación por violación de FK compuesta `fk_cust_est_establishment`. ✅ **PASS**.

3. **Protección contra Cross-Tenant Customer Spoofing:**
   - Intento de registrar `saas_customer_establishments` con `tenant_id = 2` para un `customer_id` de Tenant 1.
   - **Resultado:** PostgreSQL rechazó la operación por violación de FK compuesta `fk_cust_est_customer`. ✅ **PASS**.

4. **Validación de Dominio en Status:**
   - Intento de insertar status `'INVALID_STATUS'`.
   - **Resultado:** PostgreSQL rechazó la operación por violación de CHECK `chk_customer_status`. ✅ **PASS**.

5. **Protección de Unicidad Cliente-Sede:**
   - Intento de insertar duplicado de `(establishment_id, customer_id)`.
   - **Resultado:** PostgreSQL rechazó la operación por violación de UNIQUE `uq_customer_per_establishment`. ✅ **PASS**.

---

## 10. PRESERVACIÓN DE NODOS CERRADOS

Se verificó la integridad física y esquema de todas las tablas de los nodos cerrados (`NODO-01` a `NODO-08`) y del núcleo B2C:

- `saas_appointments` (NODO-06): Intacta ✅
- `saas_service_tickets` (NODO-08): Intacta ✅
- `saas_ticket_items` (NODO-08): Intacta ✅
- `saas_ticket_payments` (NODO-08): Intacta ✅
- `public.usuarios` (B2C / Auth Core): Intacta ✅
- `public.bookings` (B2C Core): Intacta ✅
- `public.services` (B2C Core): Intacta ✅

---

## 11. REGRESIÓN SAAS 065–073

Se ejecutó la suite de regresión integral que valida las relaciones e invariantes desde `065_saas_foundation_core.sql` hasta `073_saas_customers.sql`:
- **065 Foundation:** PASS (Tenants, Orgs, Ests, Memberships íntegros).
- **067-070 Catalog & Schedules:** PASS (Offers, Assignments, Schedules, Materializations íntegros).
- **071 Appointments:** PASS.
- **072 Service Tickets & Payments:** PASS.
- **073 Customers & Customer Establishments:** PASS.
- **Total de Regresiones:** 0 errores.

---

## 12. ALCANCE DE ARCHIVOS MODIFICADOS

- [`backend/migrations/073_saas_customers.sql`](file:///backend/migrations/073_saas_customers.sql) (Ejecutado)
- [`ncp/GO-08.17-CUSTOMER-MIGRATION-EXECUTION-AUDIT.md`](file:///ncp/GO-08.17-CUSTOMER-MIGRATION-EXECUTION-AUDIT.md) (Nuevo informe)

*(Ningún archivo de código backend, frontend, endpoints, services, ni controllers fue modificado).*

---

## 13. VEREDICTO FINAL GO-08.17

```text
============================================================
                  CUSTOMER MIGRATION 073
                  EXECUTED SUCCESSFULLY

                  PHYSICAL VALIDATION PASS

                       NO REGRESSION

       READY FOR BACKEND IMPLEMENTATION AUTHORIZATION
============================================================
```
