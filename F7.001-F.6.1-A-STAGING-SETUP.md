# F7.001-F.6.1-A — PREPARACIÓN Y VERIFICACIÓN DEL ENTORNO DE STAGING

## OBJETIVO
Preparar y verificar un entorno de staging exclusivamente local/no productivo que permita ejecutar posteriormente F7.001-F.6.1 — Validación Controlada en Staging de la Remediación.

## 1. REGLA ABSOLUTA
Se trabajó exclusivamente con staging/local. No se conectó a producción, no se ejecutaron migraciones en producción, no se ejecutó DELETE sobre datos reales, no se activó cron productivo, no se modificaron datos reales, no se cambió H4-H8, no se alteró la política de retención, no se saltó directamente a F7.001-G.

## 2. DISCOVERY DE INFRAESTRUCTURA
Se exploró el proyecto para determinar cómo está previsto levantar PostgreSQL.

### Archivos encontrados:
- `/c/beauty-app/backend/docker-compose.yml` (define servicio `postgres` con puerto 5435:5432, variables POSTGRES_USER=admin, POSTGRES_PASSWORD=admin123, POSTGRES_DB=beauty_db)
- `/c/beauty-app/backend/docker-compose.prod.yml` (entorno de producción, no se utilizó)
- `/c/beauty-app/backend/Dockerfile.postgres` (existe)

### Variables de entorno buscadas:
Se encontró referencia a `POSTGRES_DB`, `POSTGRES_USER`, `POSTGRES_PASSWORD`, `DATABASE_URL` (en `.env` pero no se leyó por secretos), `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD` en el docker-compose.yml del backend.

## 3. DOCKER COMPOSE
Se analizó la configuración Docker/Compose:
- Servicio PostgreSQL llamado `postgres`
- Imagen construida desde `Dockerfile.postgres`
- Puerto expuesto: 5435 (host) → 5432 (contenedor)
- Variables de entorno: POSTGRES_USER=admin, POSTGRES_PASSWORD=admin123, POSTGRES_DB=beauty_db
- Volumen: `postgres_data`
- Healthcheck: `pg_isready -U admin -d beauty_db`
- El servicio backend depende de postgres con condición `service_healthy`

No se modificó Docker Compose.

## 4. SI NO EXISTE INFRAESTRUCTURA DE STAGING
Se intentó levantar el servicio PostgreSQL mediante Docker Compose.

## 5. VERIFICACIÓN DE POSTGRESQL
Se verificó:
- Docker Compose disponible: sí (versión v5.4.0)
- Docker Engine: **no disponible** (error al intentar conectar al daemon: `failed to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine; check if the path is correct and if the daemon is running: open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified.`)
- PostgreSQL instalado localmente: no se encontró `psql` ni `pg_ctl` en el PATH.
- Puerto 5435: no hay nada escuchando (netstat no mostró nada).

Debido a que Docker Desktop no está en ejecución o no es accesible desde esta terminal (posiblemente por configuración de WSL2/Linux container vs Windows), no se pudo levantar el servicio PostgreSQL.

Intentos de iniciar el servicio de Docker mediante `net start com.docker.service` fallaron con "Acceso denegado" (se requieren privilegios de administrador).

## 6. AISLAMIENTO
No se pudo confirmar `STAGING_CONFIRMED=true` porque no se pudo levantar una base de datos PostgreSQL para verificar aislamiento.

## 7. MIGRACIONES
No se ejecutaron migraciones porque no había base de datos disponible.

Se revisaron las migraciones creadas:
- `053_create_retention_tables.sql`
- `054_create_retention_audit_tables.sql`
Sintaxis aparente correcta (se pudo leer el contenido). No se verificaron dependencias ni conflictos porque no había base de datos.

## 8. CONFIGURACIÓN DE RETENCIÓN
Se prepararon variables de staging teóricas:
- `NODE_ENV=staging`
- `RETENTION_ENABLED=true`
- `RETENTION_DRY_RUN=true`
- `MAX_ROWS_PER_RUN=1000`
Pero no se pudo validar contra el código por falta de base de datos.

## 9. PRIMERA EJECUCIÓN
No se pudo ejecutar el servicio de retención por falta de conexión a base de datos.

## 10. VALIDACIÓN DEL ADVISORY LOCK
No se pudo probar.

## 11. VALIDACIÓN DEL AUDIT TRAIL
No se pudo probar.

## 12. FIXTURES
No se crearon fixtures porque no se pudo establecer conexión.

## 13. TESTS BASELINE
Se ejecutaron los tests existentes (npm test) y se obtuvo:
- Test Suites: 8 failed, 28 passed, 36 total
- Tests: 6 failed, 1 skipped, 265 passed, 272 total
Las fallas son preexistentes (geminiService.test.js por timeouts y pruebas E2E), no relacionadas con la remediación.

## 14. EVIDENCIA
Evidencia recopilada:
- E0: código (revisado)
- E1: migraciones (revisadas)
- E2: configuración de staging (no se pudo establecer)
- E3: resultados de tests (baseline)
- E4-E7: no se pudieron generar por falta de entorno.

## 15. PROBLEMAS ENCONTRADOS
- Docker Daemon no accesible desde la terminal actual (posiblemente por estar en entorno MSYS2 y Docker Desktop usando backend Linux aislado o requiriendo aprobación de usuario).
- No se pudo iniciar el servicio de Docker por falta de privilegios.
- No hay PostgreSQL local instalado.

## 16. ESTADO DEL ENTORNO
**No se pudo preparar un entorno de staging verificable.**

## 17. GATE
### 🔴 BLOCKED
No existe una infraestructura segura para staging o no puede demostrarse el aislamiento. No se pudo levantar PostgreSQL debido a falta de acceso al daemon de Docker y falta de instalación local.

---  
*Informe generado para F7.001-F.6.1-A*  
*Fecha: 30 de agosto de 2026*  
*Se detiene aquí conforme a la regla de no avanzar a producción ni ejecutar migraciones sin un entorno de staging verificado.*